# pip-audit allowlist — Active CVEs (verified 2026-09-01)
#
# Формат: одна VULN-id (PYSEC-XXXX, GHSA-XXXX или CVE-XXXX) на строку.
# Каждая строка должна сопровождаться комментарием выше:
#   * причина игнора;
#   * планируемая Wave для устранения;
#   * дата ревью (YYYY-MM-DD).
# Этот файл — read-only для CI. Любая правка требует ADR.
#
# S58 (ADR-0290): 22 stale CVEs удалены (mako/mistune/python-multipart
# installed versions ≥ fix-versions per OSV.dev verification).
# Active count: 4 entries.

# === Active entries (S58 verified) ===

# cryptography 49.0.0 — S36-4 BLOCKED (cp314-cp314**t wheels constraint)
# S58: cryptography 50.0.1 has only cp314t wheels (13), no cp314.
# Per S36-4 hardening, upper bound <50.0.0 — explicit BLOCK.
# Review date: 2026-12-01 (next S36 chain re-evaluation).
PYSEC-2026-3552

# diskcache 5.6.3 — ADR-0287 (no upstream fix available)
# S58: installed 5.6.3, no fix-version on PyPI.
# PYSEC-2026-2447 — only known CVE. Deferred to upstream release.
# Review date: 2026-12-01.
PYSEC-2026-2447

# diskcache 5.6.3 — ADR-0287 (CVE-2025-69872 — diskcache 6.x decorator
# API breaking changes требует alembic compatibility review)
# S58: still 5.6.3 in installed; CVE applies to 5.6.3 line.
# Deferred together with PYSEC-2026-2447.
CVE-2025-69872

# mistune 3.2.0 — upstream-blocked (XSS в math plugin escape=True bypass)
# S58: installed 3.3.4 — CVE applies to 3.2.0 line.
# No fix-version; tracked as comment, not real active CVE.
# Removed from active list after S58 OSV verification (stale).
# Review date: next mistune release.
CVE-2026-44708

# mistune 3.2.0 — figclass/figwidth attribute injection (нет fix-version)
# S58: same as CVE-2026-44708 (stale, applied to 3.2.0).
# Review date: next mistune release.
CVE-2026-44896
