# Project memory — Discovered spillover
_Stable / historical / methodological facts extracted from main MEMORY.md to keep it under token budget. See main file ## Discovered for current items; this spillover holds the rest._

## Historical session-fact dumps

- **Real repo state at session-start** (verified 2026-07-16): branch `master`, HEAD `61f1fa0b some fixes`. Active sprint = 178 (commit messages contain `s178-m-st-1`, `s178-m-st-2`, `s178-m-st-3`, `s178-m-qw-3`). No sprint tags exist. The master prompt's "Sprint 177 closed / 808 commits / 5041+ files" framing is OUTDATED — actual commit log shows M-st-* and M-qw-* labels continuing into S178, with 29 atomic M1-M12 commits visible in the recent history for S172-S177 alone. Treat master-prompt as a rule/role spec, not as a sprint-state report. Verify any sprint-numbered claim against `git log --oneline`.
- **SEC-2 (WebSocket JWT handshake) is already DONE** since Sprint 172 (commit `a3bb7acc feat(s172-m1): ARC-001/002/003 — WS auth facade, FilteredDirectoryScan shim, tool-policy dedup`). Implementation: `src/backend/entrypoints/websocket/ws_handler.py::_authenticate_handshake` (lines 95-159) — extracts credential via `extract_credential` (subprotocol `jwt.<token>` / `apikey.<token>`, cookie `auth_session`, or `?token=...`), routes through `WSAuthenticator.authenticate_via_facade`, closes connection with WS code 1008 on missing/invalid credential. Three sources supported; JWT and API key both validated. Toggle via `WSSettings.require_auth` (default True). Test coverage: 248 LOC in `tests/unit/entrypoints/websocket/test_ws_handler.py` (incl. `test_no_credential_closes_with_1008`, `test_invalid_credential_closes_with_1008`, `test_auth_skipped_when_require_auth_false`) + 160 LOC `test_ws_auth.py` + 252 LOC `test_ws_auth_facade.py`. Master-prompt's "SEC-2 WebSocket без JWT в handshake" claim is FALSE — do NOT redo; pick a different P0.
- **`WSAuthError` (not HTTPException) is the correct error type** for WS auth failures — close code 1008 is mapped to it. The class lives in `src/backend/entrypoints/websocket/ws_auth.py:43`. WS close codes used: 1008 = policy/auth failure, 1011 = internal error, 1008 also for pool overflow and route-pool-full.
- **P0 backlog de-duplication map (final, session-3 2026-07-16)**: 10/16 master-prompt P0/P1/QW items CLOSED, 6 OPEN. **CLOSED**: SEC-2 (S172 M1.1, commit `a3bb7acc` — WS JWT handshake), SEC-3 (S172 M2, commit `451fd414` — Argon2id with per-key salt in `src/backend/core/auth/api_key_backend.py`), ARC-2 (S170 commit `a5771dc2` — `core/workflow/builder.py` imports only via facade), PERF-1 (S172 M1.2 — `fs_directory_scan.py` is a DEPRECATED shim delegating to `FilteredDirectoryScanProcessor` which uses `asyncio.to_thread` + `asyncio.wait_for`), TEST-1 (`pyproject.toml:939-946` `[tool.coverage.report] fail_under = 75`), QW-1 (S176 M11.2 commit `e5e569b7` — `audit_event_lite` adopted in `pages/45_Админ.py`, `17_Replay_Воркфлоу.py`, `16_Воркфлоу.py`), QW-2 (=PERF-1), QW-3 (S177/S178 ST-2 commit `46c1e4d2` — `.github/workflows/sprint-health.yml`), QW-5 (WIP stashed in `stash@{0}`). **OPEN (6)**: SEC-1 (`entrypoints/api/v1/endpoints/admin_plugins/endpoints.py:30-251` — 8 routes missing `Depends(require_admin_or_role)`, only feature-flag checks), ARC-1 (56 violations in `infrastructure_facade.py:139-731`, ADR-0149 incremental Top-3/sprint), ARC-3 (`core/auth/ldap_client_factory.py:30-31,101-105` lazy-imports `src.backend.services.auth.ad_directory_client`), HITL-1 (consumer-side Redis subscribe missing; producer-wiring atomic; `InMemoryHitlSignalStore.wait_for` uses `asyncio.Event`), RES-1 (4-way breaker split: core+infrastructure+entrypoints+client; middleware per-route in-memory), LOG-1 (~298 files without canonical logger), QW-4 (`core/interfaces/` 49 files orphan-audit pending), QW-6 (`tools/check_docstrings.py` ratchet baseline counts not established).
- **HITL-1 actual scope split (session-3)**: master-prompt lists HITL-1 as "busy-wait → pub/sub consumer" but the gap decomposes into (a) **producer-wiring** — 5-10 LOC additive change to `HitlService.resolve()` at `src/backend/services/workflows/hitl_service.py:340-422` (lazy import of `publish_hitl_resolved` from `hitl_pubsub.py`, wrap in `try/except`, never breaks resolve flow); (b) **consumer-side Redis subscribe** — multi-file refactor of `InMemoryHitlSignalStore.wait_for` (lines 257-272) replacing `asyncio.Event` with Redis subscriber. Producer-wiring is the atomic 1-file change; consumer-side is multi-sprint scope.
- **HITL pub/sub channel pattern (verified)**: per-tenant channel name `hitl:resolved:{safe_tenant_id}` where `safe` replaces `:` and `*` with `_`. Message body JSON shape `{signal_id, workflow_id, tenant_id, action, resolved_by, payload, event_type}`. `HitlPubSubChannel` class in `src/backend/services/workflows/hitl_pubsub.py:106-142` is the DI-friendly wrapper. Failures logged as warnings, publish returns 0 (caller never breaks).

## Methodological / verification protocols

- **Verification protocol for master-prompt P0 items (session-3 confirmation)**: before offering any master-prompt P0 as a menu choice, run a quick reality-check: (1) `git log --oneline -10 -- <file>` to find recent commits, (2) `Read` the file to confirm current state, (3) cross-reference with ADR INDEX for related decisions, (4) only then offer as a candidate. The "audit before task-pick" pattern was validated by session-3 finding 10/16 master-prompt items already closed.

## Re-derivable environment specifics (carry-forward)

- **Working-tree hygiene rule (lesson from this session):** before starting ANY new task, run `git status -s | wc -l` and `git diff --shortstat`. A non-zero result means prior agent left a WIP. The session-1 path was: (1) stash with `git stash push --include-untracked -m 'wip-sN-<context>-<YYYY-MM-DD>'`, (2) verify clean tree with `git status -s | wc -l == 0`, (3) confirm via `git stash list` that the WIP is preserved. Without this discipline, any new edit risks overwriting another agent's unfinished work — confirmed in this session (56 unstaged files, +776/-1517 LOC, 2 deletions including `admin_plugins.py` and `circuit_breakers.py`, 1 untracked `test_mixins.py`).
- **RES-1 canonical CircuitBreaker location = `src/backend/core/resilience/circuit_breaker.py`** (verified session-5). The previously-deleted `src/backend/infrastructure/clients/external/circuit_breakers.py` (in stash@{0}) has been recreated at canonical `core/resilience/` path. Future RES-1 work (split-brain resilience consolidation per master-prompt) should target this file. Aligns with ADR-0149 ARC-005 incremental Top-3-per-sprint pattern.
- **`src/frontend/admin-react/` is physically deleted from repo** (verified session-5). Confirms ADR-NEW-8 closeout: "Streamlit-only frontend (user rule S173+)" fully enforced. Production admin via REST endpoints only. Do NOT recreate any React/Vue/admin-frontend.
- **`src/backend/utilities/admin_panel/{files,orderkinds,orders,users}.py` deleted** (verified session-5). Rule 2 migration: "Бизнес-логика только в `extensions/<name>/`". Admin-panel shims moved to `extensions/core_entities/` per Sprint 71+ cleanups. `src/backend/utilities/admin_panel/` directory should now be empty or absent.
- **Pre-commit hooks directory empty in this env** (verified session-5 via `.git/hooks/` listing). No automated gates run on commit. Manual discipline required for: layer-check (`python tools/checks/check_layers.py`), secret-scan (`python tools/checks/check_secrets_simple.py`), ruff format/check. User's `make pre-commit` / `make ci` infrastructure handles these when run explicitly.

## Re-derivable from current code (Python-fact dumps)

- **Circular import pattern: `core.interfaces.__init__` ↔ `core.resilience.breaker`** (verified session-9, fixed in `core/interfaces/__init__.py`). Triggered by eager `from src.backend.core.resilience.breaker import BreakerSpec as CircuitBreakerConfig`. Chain: `core.resilience.breaker` → `core.logging` → `infrastructure.logging.factory` → `infrastructure.logging.router` → `core.interfaces.log_sink` → `core.interfaces.__init__` → back. **Standard fix**: remove eager re-export + add module-level `__getattr__` for backward-compat aliases + `TYPE_CHECKING` block for static type hints. Verified: collection goes from "ImportError partial init" to all tests collected.
- **`@runtime_checkable` is required for `isinstance(x, Protocol)` checks** (verified session-9 via CB scaffold test failure). Without it, Python raises `TypeError: Instance and class checks can only be used with @runtime_checkable protocols`. Apply to all `Protocol` subclasses used in `isinstance` calls. Single decorator, self-explanatory — no commentary block needed.

## Bootstrap patterns (deprecated-or-redundant)

- **`/tmp/venv` runtime verification pattern when project tooling unavailable** (verified session-7). Steps: (1) `python3 -m venv --without-pip /tmp/venv`, (2) download `get-pip.py` to `/tmp/`, (3) `/tmp/venv/bin/python /tmp/get-pip.py`, (4) `/tmp/venv/bin/pip install pytest,pytest-asyncio,pytest-mock,redis,purgatory,tenacity,orjson,prometheus_client,structlog,svcs,httpx,fastapi,pydantic,pydantic-settings,clickhouse-connect,joserfc,loguru,cachetools,aiofiles,sqlalchemy,asyncpg,aiosqlite,PyYAML,jsonschema,watchfiles,opentelemetry-api,opentelemetry-sdk,python-jose,psutil,confluent-kafka,e2b,pydantic-ai,langgraph,fastmcp`. ~30s bootstrap. Enables `python -m pytest` without polluting system Python. PEP 668 blocks direct `pip install --user`.
- **Plan `lockjaw-vision-rocket.md` audit delta** (verified session-9): Settings classes = **74** (plan claims 96, outdated), DSL processor file count via `find *processor*` = **44** (plan claims 253, different counting method). All other plan numbers (god-module LOC, bug locations, mixin usage, CB scaffold, core→services violations) verified accurate. Plan needs refresh on §0.1 and §0.10 only.
