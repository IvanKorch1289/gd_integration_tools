# M4 Audit — Coverage & Pre-Prod Status (2026-09-01)

> **Generated**: Sprint 88 (M4 verification per PRODUCTION_READINESS.md §M3).
> **Source**: `coverage report --precision=1` + `make pre-prod-check` + `ruff check`.
> **Refresh**: when M4 done-критерий (`pyproject.toml:fail_under = 70`) is achievable.

## Verified M4 status

### Coverage (per layer)

| Module | Coverage | Target | Status |
|---|---|---|---|
| `core/auth/*` (full) | **79.0%** | 70% | **✓ EXCEEDS** |
| `core/auth/quotas.py` | 95.5% | 70% | ✓ |
| `core/auth/quotas_protocol.py` | 90.9% | 70% | ✓ |
| `core/auth/require_sso_auth.py` | 72.6% | 70% | ✓ |
| `core/auth/saml_backend.py` | 93.7% | 70% | ✓ |
| `core/auth/sso_registry.py` | 83.6% | 70% | ✓ |
| `core/auth/sso_types.py` | 86.5% | 70% | ✓ |
| `core/auth/mobile_jwt_redis.py` | 0.0% | 70% | ✗ (TODO) |
| `core/auth/mobile_jwt_revocation.py` | 56.3% | 70% | ✗ (TODO) |

**core/auth overall: 79.0%** — **EXCEEDS 70% M4 target**.

### Other M4 gates

| Gate | Status | Notes |
|---|---|---|
| `make pre-prod-check` | TIMEOUT (143) | Heavy run; individual gates below |
| `ruff check src/` | **159 errors** (130 auto-fixable) | Pre-existing technical debt |
| `mypy src/` | FAIL | S50 audit: 30+ mypy errors (mostly type narrowing) |
| `layers` | OK | 37 legacy (S49 W3 closed 1) |
| `secrets` | TIMEOUT (143) | pip-audit run heavy |

### M4 done-критерий

`pyproject.toml:fail_under = 70` — частично достигнут (79% в core/auth, но overall coverage < 70% из-за остальных модулей).

### M4-#6: update pyproject.toml fail_under

**Decision**: keep `fail_under = 60` (current). Reaching 70% overall требует:
- 12+ hours test writing (DSL processors, services, infrastructure)
- Multi-day effort (per S48 estimate 4-6 weeks)
- Deferred to S91+ (M4 full closure)

## Sprint 88 actions

- [x] M4 coverage measurement (core/auth = 79.0%)
- [x] Pre-prod-check attempt (TIMEOUT 143)
- [x] Ruff strict check (159 errors, 130 auto-fixable)
- [x] Document M4 status в M4_AUDIT_2026-09-01.md
- [ ] Ruff auto-fix (130 auto-fixable → low risk) — 0.5h
- [ ] update pyproject.toml fail_under 60 → 70 — DEFERRED (overall coverage < 70%)

## Honest assessment

**What's done**:
- M1 (Security P0): 100% ✓
- M2 (God-objects): 100% ✓
- M3 (Dependency): 100% ✓
- M4 (Coverage ≥70%): **partial** — core/auth = 79% (exceeds), overall = 30.8% (deferred)
- M5 (Hardening): 0/10 (deferred)
- M6 (Verification): 0/N (deferred)

**Why M4/M5/M6 are deferred**:
- Test infrastructure missing (env issues: prometheus_client, fastmcp, jupyter_client not in dev-light)
- M5 items require production deploy to validate (connection pools, timeouts, graceful shutdown)
- M6 requires `make dev-light` running — not possible in this session

## Sprint 88 retro

**Sprint 88 action plan**:
1. Auto-fix 130 ruff errors (--fix flag, safe)
2. Update fail_under to 60 (already there)
3. Document M4 status

**Sprint 89+ (deferred)**:
- Write tests for DSL processors, services, infrastructure (multi-day, deferred to S91+)
- M5 hardening requires production env (deferred to dedicated sprint)
- M6 functional verification requires `make dev-light` running (deferred)