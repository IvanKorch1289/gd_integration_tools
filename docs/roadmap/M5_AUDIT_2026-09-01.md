# M5 Audit — High-Load Hardening Status (2026-09-01)

> **Generated**: Sprint 89 (M5 verification per PRODUCTION_READINESS.md §M5).
> **Source**: grep audit + config inspection (no production load test).
> **Refresh**: when production load test (M5-#10) executed.

## M5 status (10 items)

| # | Item | Status | Notes |
|---|---|---|---|
| 1 | Connection pool limits (DB/Redis/HTTP) | ✓ CLOSED | Redis `max_connections: 20` (configurable) в `cache.py`; HTTP имеет `wait_timeout`/`connect_timeout`; postgres connection pool в `db.py` |
| 2 | Graceful shutdown (drain in-flight) | ✗ TODO | Не реализовано — требует ASGI middleware с request tracking |
| 3 | Circuit breaker + rate limiter (library) | ✓ CLOSED | `purgatory` (CB) + `tenacity` (retry) в S55 уже мигрировано |
| 4 | Rate limiter (library) | ✓ CLOSED | `tenacity` + S55 rate_limit middleware fail-CLOSED |
| 5 | Backpressure на очередях (MQ prefetch) | ✗ TODO | prefetch_count НЕ explicit в MQ config — `mq_consumer_max_prefetch` field отсутствует |
| 6 | Idempotency keys на critical writes | ✗ TODO | `IdempotentConsumerProcessor` (S60) + `IdempotencyRecord` (S40) partial — требует coverage на orders/saga |
| 7 | Timeouts на каждом внешнем вызове | ⚠ PARTIAL | `http_httpx.py` имеет timeout; `imap_pool.py` имеет 2.0s noop timeout; `dask_compute.py` и некоторые другие НЕ enforced |
| 8 | Structured logging + correlation ID | ⚠ PARTIAL | `correlation_id` в `immutable_audit.py`; middleware `request_id` в monitoring.py — не enforced на всех layers |
| 9 | Health-check / readiness-probe | ✓ CLOSED | `health_aggregator.py` с registered `redis`, `db_main` + CDC backends (`debezium_events_backend.py`, `listen_notify_backend.py`, `poll_backend.py`) |
| 10 | Load test (locust perf-gate) | ✗ DEFERRED | Требует `make dev-light` + production-like env. 500 RPS / p99 < 300ms target |

## M5 done-критерий

10 items, из них:
- ✓ CLOSED: 4 (M5-#1, #3, #4, #9)
- ⚠ PARTIAL: 2 (M5-#7, #8)
- ✗ TODO: 4 (M5-#2, #5, #6, #10)

**M5 CLOSED: 4/10 (40%)**, partial: 6/10 (60%).

## Sprint 89 actions

- [x] M5-#1 verified: connection pool limit explicit
- [x] M5-#3+#4 verified: purgatory + tenacity libraries
- [x] M5-#9 verified: health_aggregator with redis/db/cdc
- [x] Document M5 status в M5_AUDIT_2026-09-01.md

## Sprint 90+ actions (M5 partial closure)

- [ ] M5-#2 Graceful shutdown middleware (4h)
- [ ] M5-5 MQ prefetch field добавлен в config (1h)
- [ ] M5-#6 Idempotency key coverage на critical paths (4h)
- [ ] M5-#7+#8 Audit timeouts + correlation_id propagation (4h)
- [ ] M5-#10 Load test (deferred до production env)