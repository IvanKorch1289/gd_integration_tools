# Production Readiness Baseline — 2026-09-02

> **Generated**: 2026-09-02 (Sprint 60+ — Pre-PRODUCTION_READINESS_FINAL plan baseline).
> **Method**: прямые команды (machine-verified output, not inherited claims).
> **Purpose**: единственная точка отсчёта для `PRODUCTION_READINESS_FINAL.md` (M1-M6).
> **Predecessor**: `BASELINE_2026-09-01.md` (S50 Sprint A reverification).

---

## 0. Verified metrics (machine-checked, 2026-09-02)

### Static analysis gates

```
$ python3 -m ruff check src/
Found 6 errors.
[*] 3 fixable with the `--fix` option.

$ python3 -m bandit -r src/ -lll
Total issues (by severity):
    Undefined: 0
    Low: 65
    Medium: 46
    High: 0
Total issues (by confidence):
    Undefined: 0
    Low: 36
    Medium: 32
    High: 43

$ python3 -m vulture src/ --min-confidence 90
(no output — 0 findings)
```

| Метрика | Факт 2026-09-02 | Факт 2026-09-01 | Δ | STATUS.md claim | Status |
|---|---|---|---|---|---|
| Ruff errors | **6** (3 auto-fixable) | 10 | **-4** | "0" | **STALE CLAIM (S60+ partial fix)** |
| Bandit HIGH (severity) | **0** | 0 | 0 | "0" | OK |
| Bandit HIGH (confidence) | **43** | 43 | 0 | — (NE disclosed) | partial disclosure |
| Vulture @≥90% | **0 findings** | 1 | **-1** | "0" | **OK (S50 Sprint C verified FP closed)** |

### Layers + tests

```
$ python3 tools/check_layers.py
Нарушений: 0 новых  (файлов: 2321; baseline: 37 legacy)

$ python3 -m pytest tests/unit/ --collect-only -q
16243 tests collected in 41.01s
```

| Метрика | Факт 2026-09-02 | Факт 2026-09-01 | Δ | STATUS.md claim |
|---|---|---|---|---|
| Layer allowlist baseline | **37 legacy** | 37 | 0 | "37" — OK |
| Layer new violations | **0** | 0 | 0 | OK |
| Tests collected | **16243** | 15862 | **+381** | (stale) |
| Tracked SKIPPED tests | 9 | 9 | 0 | (S48 W11 + croniter + secrets + moto + orders_saga) |

### Dependencies

```
$ uv run pip-audit --vulnerability-service osv
Found 4 known vulnerabilities in 2 packages
Name         Version ID              Fix Versions
cryptography 49.0.0  PYSEC-2026-3552 50.0.0
cryptography 49.0.0  PYSEC-2026-3552 50.0.0  (duplicate reporting)
diskcache    5.6.3   PYSEC-2026-2447
diskcache    5.6.3   PYSEC-2026-2447  (duplicate reporting)
```

| Метрика | Факт |
|---|---|
| CVE-affected packages (unique) | **2**: cryptography + diskcache |
| Direct CVE fix available | **1**: cryptography 50.0.0+ (BLOCKED per S36-4 hardening, ADR needed) |
| No fix available | **1**: diskcache (ADR-0287 deferral) |
| Tornado / pypdf CVEs | **0 open** (Sprint 53 not yet executed — see Sprint 53 plan) |

### Coverage (placeholder)

```
$ grep -E "fail_under" pyproject.toml
fail_under = 60
```

| Метрика | Факт |
|---|---|
| Coverage gate (pyproject.toml) | **60%** |
| Measured coverage baseline (S59) | **30.8%** (gate FAILING) |
| Sprint 67 gate status | **NOT MEASURED in this baseline run** (full `coverage run` deferred per Ponytail rule — measured baseline = S59) |
| Full coverage re-measurement | DEFERRED to M4-S1 (post-M3 to avoid false signal during churn) |

### Atomic commits velocity

```
$ git log --oneline --since="2 weeks ago" | wc -l
683
$ git log --oneline | wc -l
5163
```

~49 commits/day за последние 2 недели. Стабильный atomic-commit pattern maintained.

### P0/P1/P2 backlog (re-verified via git log)

| Item | Status | Source |
|---|---|---|
| P0 open | **0** (S49 final commit `57a396d84` "M1 22/22 P0 CLOSED") | git log |
| P1 open | **~3-5 god-object candidates (M2-#1/#2/#3 partial)** | Sprint 57/60/62/63/64 retros |
| P2 open | **0** (per STATUS.md S47 W2+W3 close-outs) | STATUS.md §TL;DR |
| **M2 progress (CORRECTED 2026-09-02)** | **15/16 (94%)** — Sprint 64 retro `fea658052`: M2-#1 AuthVerifyMixin extracted (4/13 methods). Current `auth/facade.py` = 516 LOC, 16 methods. **~9 methods still in main class** to extract. | `git log --grep="M2"` + `wc -l src/backend/core/auth/facade.py` |
| M3 progress | **STOPPED at Sprint 53** (Sprint 53 plan exists, no execution) | M3_AUDIT_2026-09-01.md |
| M4 progress | **Baseline 30.8%** (gate 60% failing) | Sprint 59 retro commit `635bbc13c` |

### Unclosed carryover from previous sprints (per user directive "перед началом спринта проверяй наличие незакрытых задач с прошлых")

| ID | Sprint origin | Status | Required action |
|---|---|---|---|
| **M2-#1 (continuation)** | Sprint 61-64 (3 partial commits: AuthResult, AuthTokenMixin, AuthVerifyMixin) | 4/13 methods extracted, 9 remaining | Continue mixin split (AuthSessionMixin or rename to specific responsibilities) — ~4-6h |
| **M2-#16 RouteBuilder Protocol** | Original M2 backlog | 2/41 mixins migrated, 39 open | Migrate remaining 39 mixins to typing.Protocol — ~8h |
| **M2-#11 sample 7/10 remaining** | Sprint 60 (3/10 done) | 7 sites still inline | Migrate remaining 7 redis_client inline imports — ~3h |
| **M3-#2 (tornado 6.5.7 → 6.5.8)** | Sprint 53 plan | Plan exists, NOT executed | `uv lock --upgrade-package tornado` + targeted tests — ~10 min |
| **M3-#3 (pypdf 6.14.2 → 6.16.1)** | Sprint 53 plan | Plan exists, NOT executed | `uv lock --upgrade-package pypdf` + tier-1+2 PDF tests — ~50 min |
| **M3-#4 (cryptography ADR-0288)** | Sprint 53 plan | ADR required before bump | Write ADR + `uv lock --upgrade-package cryptography` — ~4h |
| **M3-#5 (final pip-audit re-check)** | Sprint 53 plan | Pending | After above 3 upgrades: `uv run pip-audit` — ~5 min |
| **WIP: ruff F821 + F401 cleanup** | Post-Plan A Sprint 31 (this gap) | 7 files modified, uncommitted | Verify + commit as `fix(core): F821 + ruff polish (Post-Plan A Sprint 31)` — ~30 min |

**Note**: ruff errors dropped from 10 → 6 between baseline dates, indicating partial ruff fix landed in the gap. Remaining 6 errors include 3 auto-fixable.

### Working tree state (not mine to touch)

```
$ git status -uno
Изменения, которые не в индексе для коммита:
  изм: docs/adr/WIKI.md
  изм: src/backend/core/security/pii_tokenizer.py  (ruff F821 follow-up)
  изм: src/backend/core/security/pii_tokenizer_models.py  (ruff F821 follow-up)
  изм: src/backend/dsl/engine/processors/web.py       (ruff F401 cleanup)
  изм: src/backend/entrypoints/api/v1/routers.py      (admin_audit_replay ordering)

### Working tree state (not mine to touch)

```
$ git status -uno
Изменения, которые не в индексе для коммита:
  изм: docs/adr/WIKI.md
  изм: src/backend/core/auth/facade.py         (M2-#1 partial — AuthResult extracted)
  изм: src/backend/core/security/pii_tokenizer.py  (ruff F821 follow-up)
  изм: src/backend/core/security/pii_tokenizer_models.py  (ruff F821 follow-up)
  изм: src/backend/dsl/engine/processors/web.py       (ruff F401 cleanup)
  изм: src/backend/entrypoints/api/v1/routers.py      (admin_audit_replay ordering)
  изм: src/backend/infrastructure/notifications/adapters/express.py  (ruff I001)
  изм: src/backend/services/execution/middlewares/rate_limit_middleware.py  (ruff I001)
  изм: uv.lock  (cryptography — see М3 deferral)
```

**Note**: эти файлы — in-progress работа другой sprint-сессии (Post-Plan A Sprint 31 + M2-#1 + M2-#11). По решению пользователя НЕ трогаем в этой сессии. **Update 2026-09-02 (post-checkpoint)**: `src/backend/core/auth/facade.py` was committed by other session during the gap (no longer in uncommitted list). Confirmed via `wc -l`: now 516 LOC, 16 methods. M2-#1 partial progress: 4/13 methods extracted into mixins (AuthVerifyMixin). **9 methods remain in facade.py** to extract.

---

## 1. Открытые точки для финального плана M1-M6

На основе verified baseline:

| ID | Точка | Домен | Источник |
|---|---|---|---|
| F1 | M1 (P0 zero-out) | Security | ✅ CLOSED (S49 W11 commit `57a396d84`) |
| F2 | M2 god-объекты cleanup (P1) | Refactor | 13/16 done; remaining 3 listed в Sprint 62 retro |
| F3 | M3 dependency upgrade (CVE) | Security | STOPPED at Sprint 53 (tornado+pypdf+cryptography pending) |
| F4 | M4 coverage gate 60% → 70% | Tests | Baseline 30.8%, gate failing |
| F5 | M5 high-load hardening | Infra | NEW — нет в roadmap, требует definition |
| F6 | M6 final verification | Verification | NEW — нет в roadmap, требует definition |

---

## 2. Cross-references

- Final plan: `docs/roadmap/PRODUCTION_READINESS_FINAL.md` (эта сессия)
- Existing roadmap (M1-M4 partial): `docs/roadmap/PRODUCTION_READINESS.md`
- Prior baseline: `docs/roadmap/BASELINE_2026-09-01.md`
- M3 audit: `docs/roadmap/M3_AUDIT_2026-09-01.md`
- Sprint 53 plan (M3 execution): `docs/roadmap/SPRINT_53_PLAN.md`
- Status: `docs/STATUS.md` (sync deferred to M6 — non-final)
- Pre-prod check: `tools/checks/pre_prod_check.py` (38 gates)