"""{{ module_name }} — auto-generated .pyi stub (Sprint 14 K3 W2).

Этот файл сгенерирован ``tools/gen_dsl_stubs.py`` через runtime
introspection ``{{ class_name }}``. Не редактировать вручную —
обновляйте через ``make dsl-stubs``.
"""

from __future__ import annotations

from typing import Any, Awaitable, Callable, ForwardRef, Union
{% for line in extra_imports %}
{{ line }}
{% endfor %}


class {{ class_name }}:
{% for method in methods %}
    def {{ method.name }}{{ method.signature }} -> {{ method.return_type }}:
        """{{ method.docstring }}"""
        ...
{% endfor %}
