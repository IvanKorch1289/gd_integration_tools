"""Service-слой для feature ``{{ feature }}`` плагина ``{{ name }}`` (auto-generated)."""

from __future__ import annotations

from typing import Any

__all__ = ("{{ pascal_name }}{{ feature|capitalize }}Service",)


class {{ pascal_name }}{{ feature|capitalize }}Service:
    """Скелет сервиса ``{{ name }}.{{ feature }}``.

    Сюда переносится бизнес-логика feature'а; вызывается из
    ``plugin.py:_{{ feature }}_handler`` или из routes/<f>/route.toml step'ов.
    """

    async def execute(self, payload: dict[str, Any] | None = None) -> dict[str, Any]:
        """Echo-stub: возвращает входной payload."""
        return {"feature": "{{ feature }}", "payload": payload or {}}
