"""V11 route management — manifest, loader, lifecycle.

Пакет введён в R1.2a (ADR-043) для манифестов маршрутов
``routes/<name>/route.toml`` + ``*.dsl.yaml``. Параллельно с legacy
flat-format ``dsl_routes/*.yaml`` (Wave 4) до завершения миграции.
"""
