"""Unit-тест feature ``{{ feature }}`` плагина ``{{ name }}`` (auto-generated)."""

from __future__ import annotations

import pytest

from extensions.{{ name }}.features.{{ feature }}.service import (
    {{ pascal_name }}{{ feature|capitalize }}Service,
)


@pytest.mark.asyncio
async def test_{{ feature }}_service_echo() -> None:
    """Стартовый smoke: ``execute`` возвращает echo-payload."""
    service = {{ pascal_name }}{{ feature|capitalize }}Service()
    result = await service.execute({"x": 1})
    assert result["feature"] == "{{ feature }}"
    assert result["payload"] == {"x": 1}
