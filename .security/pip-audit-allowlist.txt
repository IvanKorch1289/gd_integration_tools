# pip-audit allowlist — Active CVEs (verified 2026-09-04)
#
# Формат: одна VULN-id (PYSEC-XXXX, GHSA-XXXX или CVE-XXXX) на строку.
# Каждая строка должна сопровождаться комментарием выше:
#   * причина игнора;
#   * планируемая Wave для устранения;
#   * дата ревью (YYYY-MM-DD).
# Этот файл — read-only для CI. Любая правка требует ADR.
#
# S58 (ADR-0290): 22 stale CVEs удалены (mako/mistune/python-multipart
# installed versions ≥ fix-versions per OSV.dev verification).
# S96 (ADR-0290 addendum): PYSEC-2026-3552 (cryptography) удалена —
# uv.lock поднят до 50.0.1 (коммит 97230556d; S58 обновлял только venv,
# lock оставался на 49.0.0 — регрессия M3, закрыта в DEP1). Mistune
# CVE-2026-44708/44896 ID-строки удалены — их же комментарии S58 помечали
# их stale («Removed from active list»), ID оставались по ошибке и
# маскировали потенциальные находки.
# Active count: 2 entries.

# === Active entries (S96 verified) ===

# diskcache 5.6.3 — ADR-0287 (no upstream fix available)
# S58: installed 5.6.3, no fix-version on PyPI.
# PYSEC-2026-2447 — only known CVE. Deferred to upstream release.
# Review date: 2026-12-01.
PYSEC-2026-2447

# diskcache 5.6.3 — ADR-0287 (CVE-2025-69872 — diskcache 6.x decorator
# API breaking changes требует alembic compatibility review)
# S58: still 5.6.3 in installed; CVE applies to 5.6.3 line.
# Deferred together with PYSEC-2026-2447.
CVE-2025-69872
