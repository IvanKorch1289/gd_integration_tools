# M3 Audit — Dependency Update Status (2026-09-01)

> **Generated**: Sprint 52 (M3 dependency update), per `PRODUCTION_READINESS.md` §M3.
> **Method**: `uv pip list --outdated` + `uv run pip-audit`.
> **Refresh**: при каждом dependency bump.

## Статистика

| Метрика | Значение | Команда |
|---|---|---|
| Outdated packages | 108 | `uv pip list --outdated \| wc -l` |
| CVE-affected packages | 4 (cryptography, pypdf, tornado, diskcache) | `uv run pip-audit` |
| Direct CVE fix available | 3 (cryptography 50.0.0, pypdf 6.16.1, tornado 6.5.8) | same |
| No fix available | 1 (diskcache 5.6.3 PYSEC-2026-2447) | same |

## CVE inventory (from pip-audit)

| Package | Version | CVE ID | Fix | Action |
|---|---|---|---|---|
| cryptography | 49.0.0 | PYSEC-2026-3552 | 50.0.0 | **BLOCKED** (pyproject `<50.0.0` per S36-4 hardening); ADR needed |
| pypdf | 6.14.2 | PYSEC-2026-3655/3656 + CVE-2026-84309/84310/84311 | 6.16.1 | Update permitted (no breaking API for our usage) |
| tornado | 6.5.7 | GHSA-wwv5-g3v4-889x + GHSA-8423-8fgw-73vq | 6.5.8 | Update permitted (patch) |
| diskcache | 5.6.3 | PYSEC-2026-2447 | none | **Deferral** — track upstream fix |

## Blocked: cryptography bump

`pyproject.toml` line 25-29:
```
"cryptography>=49.0.0,<50.0.0",  # upper bound <50.0.0
```
PYSEC-2026-3552 requires 50.0.0+. Per S36-4 hardening, this upper bound was
explicit (PYSEC-2026-3552 was open at the time).

Resolution:
1. ADR-0XXX: "cryptography upper bound lift 50→51" with risk analysis.
2. uv.lock upgrade (cryptography 49.0.0 → 50.0.0+).
3. Run make lint-strict && make test (validate SSL/TLS code paths).

## Plan

- M3-#1 (this file): baseline — DONE
- M3-#2: cryptography ADR + upgrade — **deferred to Sprint 53** (requires
  careful test pass)
- M3-#3: minor/patch bulk upgrade (pypdf, tornado) — **Sprint 52**
- M3-4: ADR for pinned major — **Sprint 52**
- M3-5: pip-audit final re-check — **Sprint 53**

## Sprint 52 scope (per plan)

1. Minor/patch upgrade pypdf + tornado (CVE-affected).
2. Add ADR for diskcache deferral (no fix available).
3. Verify via `make lint-strict && make test`.

Cryptography upgrade (M3-#2) explicitly deferred to Sprint 53 — requires
careful test pass + ADR.