# Sprint 18 — Remaining Tasks & New Features

## Текущий статус проекта

### Выполнено (Sprint 1-17)
- ✅ Удалён pybreaker мёртвый код (545 строк)
- ✅ Исправлены Python 2 except syntax (86 экземпляров)
- ✅ Созданы facade: ResilienceFacade, SchedulerFacade, SecretsFacade
- ✅ Реализованы SkillRegistry exports: MCP, LangGraph, OpenAI tools
- ✅ Все top-level layer violations исправлены
- ✅ Реализован Agent hot-reload
- ✅ Добавлены DSL методы: with_connection_pool, with_reconnection, region_routing, supervisor
- ✅ Добавлены тесты и lint gates
- ✅ Проведён полный аудит инфраструктурного слоя

### Оставшиеся задачи

#### P1 — Критические
1. **OIDC SSO реализация** — abstract base class exists, implementation planned for S126+
2. **Agent tool policy integration** — infrastructure exists, not integrated into AIGateway

#### P2 — Средние
3. **DSL region_routing processor** — builder method exists, processor needed
4. **DSL supervisor processor** — builder method exists, processor needed
5. **SecretsFacade + AuditFacade** — planned in Sprint 1, not implemented

#### P3 — Низкие
6. **Video modality** — scaffold, marked "staged for S12"
7. **DSL-level connection_pool processor** — builder method exists, processor needed
8. **DSL-level reconnection processor** — builder method exists, processor needed

## План Sprint 18

### Wave 1: DSL Processors (P2)
- Создать `RegionRoutingProcessor` в `dsl/engine/processors/`
- Создать `SupervisorProcessor` в `dsl/engine/processors/`
- Добавить тесты

### Wave 2: Facades (P2)
- Создать `SecretsFacade` в `services/secrets/`
- Создать `AuditFacade` в `services/audit/`
- Добавить тесты

### Wave 3: Review + CHANGELOG + commit
- Обновить CHANGELOG
- Закоммитить изменения
