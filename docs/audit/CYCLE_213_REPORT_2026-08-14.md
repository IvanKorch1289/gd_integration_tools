# CYCLE 213 — Frontend Facade Regression Test (2026-08-14)

**HEAD**: `2532c9b` + cycle 207/208/209 + 5df08e40 (intervening)
**Агент**: Kimi Code CLI, swarm mode

---

## 🎯 Finding: cycle 209 inline changes reverted by 5df08e40

Cycle 209 (предыдущий) пытался "inline" 3 pure utility функции (`load_pipeline_from_yaml`, `Pipeline`) в `_editor/{yaml_sync,properties,visual/tab_canvas}.py` путём direct import из `src.backend.dsl.*`.

**Проблема**: commit `5df08e40 feat(frontend): cycle 206 — migrate _editor/ direct dsl imports to facade` (intervening) **re-вёртнул** эти изменения обратно к facade импортам. Причина — layer-checker (`tools/check_layers.py`) запрещает frontend импортировать `src.backend.dsl.*` напрямую (только через `core.api` facade).

Per AGENTS.md: "extensions импортируют ТОЛЬКО `src.backend.sdk` + `src.backend.core.api`". Frontend формально — НЕ extension, но layer-checker применяет те же правила (S6 W2 / R3.10d / S36).

**Итоговое состояние** (после 5df08e40):
- ✅ 10 HTTP-migrated файлов (cycle 207-208) — committed
- ❌ 3 inlined pure utility — **reverted by 5df08e40** (т.е. реально 7 файлов всё ещё используют facade)
- ✅ 4 documented intentional (M7 backlog) — unchanged

---

## ✅ Out-of-scope: 4 coherent slices done

| Slice | Что | Tests | Файлов |
|---|---|---|---|
| **1** | Regression tests для 5 fix'ов (NEW-1, -1b, -1c, -8, -9) | 17/17 PASS | 5 |
| **2** | Tests для новых HTTP client methods (cycle 208) | 11/11 PASS | 1 |
| **3** | Regression test для NEW-3a (`/asyncapi` bridge) | 6/6 PASS | 1 |
| **4** | Frontend facade regression test (targeted) | 3/3 PASS | 1 |

**ИТОГ**: 37/37 tests PASS, lint clean.

### Slice 4 детали — `test_no_frontend_facade_regression.py`

Защищает cycle 207-208 migration:
- 10 HTTP-migrated файлов НЕ ДОЛЖНЫ импортировать facade символы, имеющие HTTP equivalents
- Targeted check: парсит `from src.backend.core.frontend_facade import X, Y, Z` и проверяет каждый `X`, `Y`, `Z` против `_SYMBOLS_WITH_HTTP_EQUIVALENT` set
- Symbol list: `get_saga_history`, `list_workflow_templates`, `get_ai_cost_snapshot`, `get_global_registry`, `list_route_ids`, `list_audit_records`, `list_recent_trace_events`
- Если кто-то re-импортирует facade для HTTP-эквивалентного символа — test падает с понятным message

Также проверяет:
- `test_total_migrated_files_count`: 10 файлов в forbidden list (sync с FRONTEND_FACADE_MIGRATION_FINAL.md)
- `test_documented_intentional_files_have_facade_docstring`: 4 documented файла имеют explicit comment с обоснованием (intentional / DEEP_AUDIT / R3.10d / M7)

### 3 вновь отмеченных facade imports (для M7 backlog)

Per test findings, эти файлы используют facade для символов БЕЗ HTTP equivalents (legitimate):
- `33_DSL_Шаблоны.py` — `WorkflowDeclaration, to_mermaid` (no HTTP endpoint)
- `_groups/dsl/dsl_templates/workflow_templates_tab.py` — `search_workflow_templates` (TODO)
- `_editor/workflow_diff.py` — `compute_step_diff, to_graphviz` (pure utility, no HTTP)

**M7 backlog**: добавить backend endpoints + client methods для этих символов.

---

## 📝 Изменённые файлы (cycle 213)

```
tests/unit/frontend/test_no_frontend_facade_regression.py | 195 lines (targeted facade regression)
docs/audit/CYCLE_213_REPORT_2026-08-14.md                | NEW
```

### Cumulative out-of-scope tests (cycle 211-213)

```
tests/unit/services/core/base/test_helpermethods_fix.py         | 89 lines  (NEW-1)
tests/unit/core/di/test_module_registry_repos_fix.py           | 80 lines  (NEW-1b)
tests/unit/services/core/base/test_crud_mixin_list.py          | 110 lines (NEW-1c)
tests/unit/dsl/commands/setup/test_registers_users_fix.py       | 76 lines  (NEW-8)
tests/unit/infrastructure/workflow/test_worker_startup_fix.py   | 73 lines  (NEW-9)
tests/unit/frontend/api_clients/test_workflows_capability_cycle208.py | 220 lines (cycle 208)
tests/unit/plugins/composition/test_asyncapi_bridge_fix.py     | 196 lines (NEW-3a)
tests/unit/frontend/test_no_frontend_facade_regression.py     | 195 lines (cycle 213)
```

**Total: 8 test files, 37/37 PASS**

---

## ⚠️ Урок (per Ponytail + AGENTS.md)

**"Изучить существующий паттерн перед добавлением нового модуля"** — применимо к тестам тоже. Cycle 209 inline changes не учли, что layer-checker запрещает `frontend → src.backend.dsl.*` direct imports, и были re-вёртнуты последующим коммитом `5df08e40`.

Test slice 4 (`test_no_frontend_facade_regression.py`) предохраняет от re-импорта facade для HTTP-эквивалентных символов (правильный enforcement). Inline changes для non-HTTP символов остаются в M7 backlog (legitimate, но требуют новых endpoints).

---

## 🔄 Open issues (multi-session, не блокеры)

- **M7**: 4 service-object файла → backend proxy endpoints (3-5 дней)
- **gRPC RPC**: Cython `aio/server.pyx.pxi:838` (image rebuild, 5-10 мин)
- **rabbitmq/AMQP в compose** (architectural)
- **Image rebuild Dockerfile HEALTHCHECK** (cosmetic)

---

## ✅ Validation (per AGENTS.md "make lint && make test")

- **ruff check** на 8 test файлов: All checks passed
- **pytest (cumulative 37 out-of-scope tests)**: 37/37 PASS
- **HTTP functional**: orders.list → 200, gRPC server UP, workflow e2e RUNNING
- **Frontend state**: 10 HTTP-migrated + 3 reverted (cycle 209) + 4 documented intentional = 17/17 (per FRONTEND_FACADE_MIGRATION_FINAL.md corrected)
