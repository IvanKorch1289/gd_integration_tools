openapi: 3.0.3
info:
  title: KeyedAPI
  version: 1.0.0
paths:
  /data:
    get:
      operationId: getData
      responses:
        "200": {description: ok}
components:
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      in: header
      name: X-API-Key
security:
  - apiKeyHeader: []
