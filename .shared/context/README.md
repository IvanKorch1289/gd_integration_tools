# context/ — общий bootstrap для обоих агентов

> **Наполняется в фазе 3.** Сейчас — placeholder.
>
> Идея: единый `BOOTSTRAP.md`, который ОБА агента читают при старте сессии:
>
> ```bash
> # Claude: через CLAUDE.md
> # Kimi:   через AGENTS.md
> # Оба файла будут указывать на этот BOOTSTRAP.md
> ```
