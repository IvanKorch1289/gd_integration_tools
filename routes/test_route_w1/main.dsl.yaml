route_id: test_route_w1
source:
  http:
    method: POST
    path: /api/v1/CHANGEME
steps:
- # D-AUDIT-B-103 fix (cycle 1): broken call_function step удалён (модуль
  # не существует — route — pure YAML demo).
- http_call:
    url: https://api.test/sink
    method: POST
- audit:
    action: test_route_w1.processed
- to:
    response:
      code: 200
      body: ${body}
