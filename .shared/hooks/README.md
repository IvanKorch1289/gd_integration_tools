# hooks/ — общие git pre-commit, session-start, session-close

> **Наполняется в фазе 4.** Сейчас — placeholder.
>
> - `pre-commit-graphify.sh` — переедет из `.claude/scripts/post-bash-graphify.sh`
> - `session-start.sh` — пишет в `vault/SESSIONS.md` (open entry)
> - `session-close.sh` — пишет в `vault/SESSIONS.md` (close entry)
