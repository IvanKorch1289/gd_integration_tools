# WAF-coverage allowlist (V15 R-V15-5; ADR-0050, ADR-0053, ADR-0061).
#
# Файл-пути (относительно корня репозитория) известных :internal
# callsite'ов прямого httpx.AsyncClient/Client. Каждая запись —
# легитимное исключение, обоснованное ADR/комментом в коде или
# baseline-долгом, ожидающим миграции на OutboundHttpClient.
#
# Формат:
#   src/backend/<path>/<file>.py  # owner=<team> | target=<sprint> | reason=<note>
#
# Пустые строки и строки начинающиеся с '#' игнорируются. Inline-
# комментарии после пути обрезаются по первому '#'.
# `--strict` режим (CI/release-gate) игнорирует этот файл.
#
# Sprint 10 закрытие (2026-05-19): 12 S10 файлов мигрированы на
# OutboundHttpClient и удалены из allowlist. 3 файла без httpx
# (search_providers/vault_refresher/polling) тоже удалены — они
# больше не нарушители. Остались 6 S11 baseline-записей.

# === S11 ownership (baseline, target=s11) ===
src/backend/core/security/vault_cipher.py  # owner=K1 | target=s11 | reason=Vault namespace capability
src/backend/infrastructure/clients/storage/clickhouse.py  # owner=K2 | target=s11 | reason=non-OAS client extension
src/backend/infrastructure/clients/external/express_bot.py  # owner=K1 | target=s11 | reason=bot-specific HTTP semantics
src/backend/infrastructure/clients/external/telegram_bot.py  # owner=K1 | target=s11 | reason=bot-specific HTTP semantics
src/backend/infrastructure/policy/opa.py  # owner=K1 | target=s11 | reason=OPA-sidecar abstraction
src/backend/infrastructure/resilience/components/express_chain.py  # owner=K1 | target=s11 | reason=resilience-chain extraction
