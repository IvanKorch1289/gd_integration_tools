# Baseline — Sprint 49 (2026-08-31)

> **Source**: ШАГ 0 reverification из `PRODUCTION_READINESS.md` (M1-M6 closed plan).
> **Method**: прямые команды (не заявленные цифры).
> **Refresh**: при каждой M-iteration sync.

## Verified метрики

| Метрика | Значение | Команда |
|---|---|---|
| Layer allowlist новых нарушений | 0 (2311 файлов, 37 legacy) | `python3 tools/check_layers.py` |
| Cross-domain violations | 1 reverse (worker.py:172) + 83 dsl→infrastructure inline | `grep -rn 'from src.backend.plugins' src/backend/infrastructure/`, `grep -rn 'from src.backend.infrastructure' src/backend/dsl/engine/processors/` |
| Ruff errors | 9 (6 auto-fixable) | `ruff check src/` |
| TODO/FIXME/HACK/XXX | 13 (info-level) | `grep -rn 'TODO\|FIXME\|HACK\|XXX' src/backend/ --include='*.py'` |
| Vulture @>=90% | 1 unused variable | `vulture src/backend/ --min-confidence 90` |
| Velocity (2 недели) | 598 commits | `git log --oneline --since='2 weeks ago' \| wc -l` |
| McpAuthMiddleware orphan | 1 reverse import (auth_middleware.py:2) | `grep -c 'McpAuthMiddleware' src/backend/entrypoints/mcp/*.py` |
| Atomic commits S48 | 36 | `git log --oneline --grep='S48\|M1\|M2'` |

## False claims (STATUS.md stale)

1. **STATUS.md "P0 open = 0"** — реальный backlog 18 P0 (S48 swarm audit), ~13 после частичного закрытия.
2. **STATUS.md "Production readiness ~96%"** — не перепроверено, считать aspirational.
3. **STATUS.md "Coverage 60%"** — фактический baseline требует measurement (`coverage run` не запускался в этой сессии).

## Backlog (S48 audit + M1-M6 plan)

| Tier | Count | Source |
|---|---|---|
| P0 | 18 → 13 (после S48 W1-W17) | S48 W1 retro §Следующая итерация |
| P1 | 60 | S48 W1 retro |
| P2 | 50 | S48 W1 retro |
| M1 new | 6 (после W1-W17 dedup) | Plan M1 |
| M2 | 16 god-объекты + custom→lib | Plan M2 |
| M3-M6 | 32 tasks total | Plan M3-M6 |

## Known technical debt

- `mobile_jwt_revocation.py:197-225` — no-op parameters (revocation_store, rate_limiter), Phase 3 impl deferred
- `infrastructure/workflow/worker.py:172` — reverse layer violation
- 83 inline `from src.backend.infrastructure` imports in dsl/engine/processors
- McpAuthMiddleware REMOVED in cycle 217 (D-AUDIT-20811), defense-in-depth потерян
- notification_hub (deprecated) still imported by 5 consumers

## Sprint 49 target

M1 P0 zero-out: 5 задач (см. `PRODUCTION_READINESS.md` §M1 + §Sprint 49).