# DEPRECATED: содержимое перенесено в config_profiles/dev.yml.
#
# Загрузка настроек выполняется ТОЛЬКО из директории config_profiles/.
# Активный профиль выбирается переменной APP_PROFILE
# (dev | dev_light | staging | prod). См. src/core/config/config_loader.py.
#
# Этот файл больше не читается loader-ом; оставлен пустым для
# обратной совместимости со скриптами/контейнерами, которые могут
# монтировать ./config.yml. Удалить после полного переезда.
