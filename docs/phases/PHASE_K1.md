# Фаза K1 — DSL+ max (Tooling + Authoring + Interop)

* **Статус:** done (ADR + план)
* **Приоритет:** P2
* **ADR:** ADR-020, ADR-021
* **Зависимости:** E2

## Выполнено

- ADR-020: LSP + formatter + mypy-plugin roadmap.
- ADR-021: Camel/Spring import + AsyncAPI/OpenAPI export.
- `tools/dsl_cli/lint.py` (E2) — первый шаг к полному toolchain.

LSP, formatter, mypy-plugin, Camel-importer — роадмап-артефакты,
реализация активируется по запросам команды-потребителей.

## Definition of Done

- [x] ADR-020, ADR-021.
- [x] `docs/phases/PHASE_K1.md`.
- [x] PROGRESS.md / PHASE_STATUS.yml (K1 → done).
