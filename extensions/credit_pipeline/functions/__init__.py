"""Call-function helpers credit_pipeline (scaffold).

TODO Team T3 (Sprint 8+):
* normalize.py — нормализация ответов внешних API;
* rules.py — apply_rules для DSL ``call_function('extensions.credit_pipeline.functions.rules:apply_rules')``.

R-V15-6: 80% YAML / 20% Python. Кастомная логика — через ``call_function``
без обёрток в Action.
"""

from __future__ import annotations

__all__: tuple[str, ...] = ()
