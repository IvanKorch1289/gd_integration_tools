# route.toml (V11): manifest for route 'test_route_w1' (S33 W1 wizard).
name = "test_route_w1"
version = "0.1.0"
requires_core = ">=22.0,<23"
capabilities = ["net.outbound", "db.write", "audit.write"]
tenant_aware = true
feature_flag = { enabled = true, gate = "test_route_w1_enabled" }
slo = { p95_ms = 500, timeout_ms = 5000 }
schedule = "never"

[feature_flags]
default = ["default"]
