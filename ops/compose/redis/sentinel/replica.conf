# Redis replica configuration (S60 W1 local development).
# Used by docker-compose.redis-sentinel.yml for local Sentinel testing.

# Persistence
appendonly yes
# Network
appendfsync everysec
bind 0.0.0.0
# local dev only
protected-mode no
