"""Placeholder: schema_registry_client module moved to backend. S30 carryover."""

from __future__ import annotations

import pytest

pytest.skip(
    "schema_registry_client moved to backend — test needs migration; S30 carryover",
    allow_module_level=True,
)
