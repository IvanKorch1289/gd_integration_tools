{
  "$defs": {
    "CapabilityRef": {
      "additionalProperties": false,
      "description": "Декларация одной capability с опциональным scope-glob.\n\nСоответствует таблице ``[[capabilities]]`` в `plugin.toml` /\n`route.toml`. ``name`` валидируется грамматикой\n:data:`CAPABILITY_NAME_PATTERN`; ``scope`` — свободная строка,\nинтерпретация делегируется :class:`ScopeMatcher` runtime-gate'а.",
      "properties": {
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "scope": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Scope"
        }
      },
      "required": [
        "name"
      ],
      "title": "CapabilityRef",
      "type": "object"
    },
    "_IPRestrictionModel": {
      "additionalProperties": false,
      "description": "Pydantic-модель для ``[security.ip_restriction]`` в route.toml.",
      "properties": {
        "allowed_ips": {
          "description": "Разрешённые IP/CIDR для доступа к маршруту.",
          "items": {
            "type": "string"
          },
          "title": "Allowed Ips",
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "Включено ли ограничение.",
          "title": "Enabled",
          "type": "boolean"
        },
        "path_pattern": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Glob-паттерн пути. Если не задан — /api/v1/auto/<route_name>.",
          "title": "Path Pattern"
        }
      },
      "title": "_IPRestrictionModel",
      "type": "object"
    },
    "_RouteTimeoutModel": {
      "additionalProperties": false,
      "description": "Pydantic-обёртка над :class:`RouteTimeoutSpec` для парсинга TOML.\n\nИспользует ``extra=\"forbid\"`` чтобы поймать опечатки в ``[timeout]``\nсекции на этапе load (вместо silent ignore).",
      "properties": {
        "connect": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Connect"
        },
        "read": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Read"
        },
        "total": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Total"
        },
        "write": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Write"
        }
      },
      "title": "_RouteTimeoutModel",
      "type": "object"
    },
    "_SecurityModel": {
      "additionalProperties": false,
      "description": "Pydantic-модель для секции ``[security]`` в route.toml.\n\nИспользует ``extra=\"forbid\"`` чтобы поймать опечатки в полях\nсекции на этапе load.",
      "properties": {
        "ip_restriction": {
          "anyOf": [
            {
              "$ref": "#/$defs/_IPRestrictionModel"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-route IP-ограничения."
        },
        "requires_permission": {
          "description": "Список required permissions в формате \"role:<role_name>\" или \"scope:<scope_name>\". При route_authz_requires_permission=True AuthorizationGateway проверяет наличие всех перечисленных permissions у principal перед dispatch на route.",
          "items": {
            "type": "string"
          },
          "title": "Requires Permission",
          "type": "array"
        }
      },
      "title": "_SecurityModel",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Манифест маршрута V11 (``routes/<name>/route.toml``).\n\nСм. ADR-043 для полного описания формата, lifecycle и\ninvariant'а ``route.capabilities ⊆ union(plugins.capabilities)``.\n\nAttributes:\n    name: snake_case-имя маршрута; совпадает с каталогом\n        ``routes/<name>/``.\n    version: SemVer-строка маршрута.\n    requires_core: PEP-440 SpecifierSet — диапазон версий ядра.\n    requires_plugins: Mapping ``plugin_name → SemVer-spec`` для\n        плагинов, чьи Processor/Source/Sink/Action использует\n        pipeline.\n    tenant_aware: Если ``True`` — pipeline сознательно работает\n        в TenantContext.\n    feature_flag: ``True``/``False`` (статически вкл./выкл.),\n        ``str`` (имя ENV или dotted-path к\n        ``IFeatureFlagProvider``), ``None`` (по умолчанию вкл.).\n    tags: Тэги для admin-grouping и DSL-Linter.\n    description: Опц. человекочитаемая аннотация.\n    pipelines: Список path'ей ``*.dsl.yaml`` относительно\n        ``routes/<name>/`` (главный — первый, остальные — fragments).\n    capabilities: Декларация runtime-gate (см. ADR-044).\n        Должна быть подмножеством объединения capabilities\n        требуемых плагинов + публичных capabilities ядра\n        (проверка в :class:`RouteLoader`, а не в pydantic).\n    security: Опц. секция ``[security]`` с requires_permission.\n        K3 S19 W3: route_authz_requires_permission feature flag\n        активирует проверку permissions через AuthorizationGateway.\n    timeout: Конфигурация таймаутов (connect / read / write / total).",
  "properties": {
    "capabilities": {
      "default": [],
      "items": {
        "$ref": "#/$defs/CapabilityRef"
      },
      "title": "Capabilities",
      "type": "array"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Description"
    },
    "feature_flag": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Feature Flag"
    },
    "name": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Name",
      "type": "string"
    },
    "pipelines": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Pipelines",
      "type": "array"
    },
    "requires_core": {
      "minLength": 1,
      "title": "Requires Core",
      "type": "string"
    },
    "requires_plugins": {
      "additionalProperties": {
        "type": "string"
      },
      "title": "Requires Plugins",
      "type": "object"
    },
    "requires_workflows": {
      "additionalProperties": {
        "type": "string"
      },
      "title": "Requires Workflows",
      "type": "object"
    },
    "security": {
      "anyOf": [
        {
          "$ref": "#/$defs/_SecurityModel"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "tags": {
      "default": [],
      "items": {
        "type": "string"
      },
      "title": "Tags",
      "type": "array"
    },
    "tenant_aware": {
      "default": false,
      "title": "Tenant Aware",
      "type": "boolean"
    },
    "timeout": {
      "anyOf": [
        {
          "$ref": "#/$defs/_RouteTimeoutModel"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "version": {
      "minLength": 1,
      "title": "Version",
      "type": "string"
    }
  },
  "required": [
    "name",
    "version",
    "requires_core",
    "pipelines"
  ],
  "title": "RouteManifestV11",
  "type": "object"
}
