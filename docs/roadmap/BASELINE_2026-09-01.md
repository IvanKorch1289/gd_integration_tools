# Production Readiness Baseline — 2026-09-01

> **Generated**: 2026-09-01 (Sprint 50, Sprint A — Pre-M1 baseline fix)
> **Owner**: Coordinator (Sprint A execution)
> **Purpose**: Точка отсчёта для Plan A (M1-M6). Все цифры — direct command output, не заявленные.

---

## 0. Verified metrics (machine-checked)

### Static analysis gates

```
$ python3 -m ruff check src/
Found 10 errors.
[*] 7 fixable with the `--fix` option.

$ python3 -m bandit -r src/ -lll
Total issues (by severity):
    Undefined: 0
    Low: 65
    Medium: 46
    High: 0
Total issues (by confidence):
    Undefined: 0
    Low: 36
    Medium: 32
    High: 43

$ python3 -m vulture src/ --min-confidence 90
src/backend/core/auth/mobile_jwt_revocation.py:202: unused variable 'revocation_store' (100% confidence)
```

| Метрика | Факт | STATUS.md claim | Status |
|---|---|---|---|
| Ruff errors | **10** (7 auto-fixable) | "0" | **STALE CLAIM** |
| Bandit HIGH (severity) | **0** | "0" | OK |
| Bandit HIGH (confidence) | **43** | — (NE disclosed) | **partial disclosure** |
| Vulture @≥90% | **1 finding** | "0" | **STALE CLAIM** |

### Layers + tests

```
$ python3 tools/check_layers.py
Нарушений: 0 новых  (файлов: 2311; baseline: 37 legacy)

$ python3 -m pytest tests/unit/ --collect-only -q
15862 tests collected in 28.09s
```

| Метрика | Факт | STATUS.md claim |
|---|---|---|
| Layer allowlist baseline | **37 legacy** | "38" (stale — S49 W3 removed 1 stale entry) |
| Layer new violations | **0** | OK |
| Tests collected | **15862** | (stale — было 15428 в S49 W11) |

### Dependencies

```
$ pip list --outdated 2>/dev/null | wc -l
138
```

| Метрика | Факт |
|---|---|
| Outdated packages | **138** (Babel 2.10.3 → 2.18.0, Pydantic, FastAPI, etc.) |
| Critical CVE | (требует `pip-audit` / `safety` — отдельная проверка) |

### P0/P1/P2 backlog (re-verified)

| Item | Status |
|---|---|
| P0 open | **4-7 OPEN** (NOT 0 as STATUS.md claimed) |
| P0 closed S48-S49 | 29/33 (#1, #2, #3, #5, #6, #7, #8, #10, #11, #12, #14, #15, #16, #19, #28, #29, #30, #33, #4) |
| P0 remaining (S48 W1 backlog) | #9 S3 silent error swallow, #17 notification_hub deprecation, #22 frontend_facade, #23-27 Frontend (4 items), #31 mobile_jwt_revocation no-op stores |
| P0 false positives | #13 (action_dispatcher eager), #32 (saml_sp_initiated flag misnamed) |
| P1 open | ~60 (top-10 в S48 W1 retro) |
| P2 open | ~50 |

### Coverage (placeholder — full run не сделан в этой baseline)

```
$ # coverage run --source=src/backend -m pytest tests/unit/ -q
# → ~13% full project coverage per S44 W32 honest re-eval (pre-S49)
```

---

## 1. FALSE CLAIM corrections (per AGENTS.md "Verify before plan")

| Claim | Reality | Source |
|---|---|---|
| `STATUS.md L17: Bandit HIGH = 0` | **PARTIAL**: severity=0 OK, confidence=43 NOT disclosed | `bandit -r src/ -lll` |
| `STATUS.md L13: P0 open = 0` | **STALE**: 4-7 P0 OPEN после S49 close-outs | `git log --grep="swarm-48"` + manual diff |
| `STATUS.md L15: Ruff errors = 0` | **STALE**: 10 errors реально | `ruff check src/` |
| `STATUS.md L17: Vulture @≥90% = 0` | **STALE**: 1 finding реально | `vulture src/` |
| `STATUS.md L18: Layer allowlist = 38` | **STALE**: 37 после S49 W3 | `check_layers.py` |

---

## 2. Atomic commits velocity

```
$ git log --oneline --since="2 weeks ago" | wc -l
603
$ git log --oneline | wc -l
5066
```

~43 commits/day за последние 2 недели (S48-S49 серия). Стабильный atomic-commit pattern.

---

## 3. Sprint velocity analysis (S48-S49 retro breakdown)

| Source | S48 commits | S49 commits | Total |
|---|---|---|---|
| Coverage ratchet files (cov+29..+40) | ~30 | ~50 | ~80 |
| P0 fixes (swarm-48) | ~25 | ~15 | ~40 |
| Test infra (deps, skips) | ~15 | ~10 | ~25 |
| Misc + cleanup | ~10 | ~5 | ~15 |
| **TOTAL S48-S49** | **~80** | **~80** | **~160** |

~80 commits per sprint at ~25h/week velocity = ~3 commits/hour sustained.

---

## 4. Cross-references

- Plan: `/home/user/.kimi-code/sessions/wd_gd_integration_tools_3de2ac373651/session_662bd9ad-c9a0-465a-9d6f-436172fba5c4/agents/main/plans/pantha-raven-siryn.md`
- Existing roadmap: `docs/roadmap/PRODUCTION_READINESS.md` (M1-M4, will be updated post-Sprint A)
- Prior swarm audit: `docs/retros/SPRINT_48_W1_SWARM_AUDIT.md` (455+ строк, P0 backlog source-of-truth)
- Status: `docs/STATUS.md` (corrected in Sprint A — 4 FALSE CLAIMs retracted)
