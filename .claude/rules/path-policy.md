Все project-relative пути интерпретировать относительно корня проекта, а не относительно расположения файла внутри `.claude/`.
Для shell-скриптов и hooks при необходимости явно вычислять root проекта перед записью файлов.