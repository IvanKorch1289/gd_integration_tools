# Domain A7-DSL-Engine-Processors — независимый аудит (cycle 1)

> Дата: 2026-08-06
> Агент: A7-DSL-Engine-Processors
> Baseline (cycle-1 Phase-1): commit `b69d6b49bc62918a02e47dc20ab81615fd8500b1`
> HEAD анализа: `7f3d94a388199c136bd7b90fa73d3b5a1217d4f7` (S184-w4 retrospective + 4 фикса в working tree)
> Метод: прямая верификация кода (Read/Grep/runtime-check через `.venv/bin/python`).
> Никаких markdown-документов как источника фактов — все цитаты кода взяты из исходников.
> Scope: `src/backend/dsl/engine/**`, `src/backend/dsl/registry/**`, подсчёт processor-классов во всём `src/backend/dsl/engine/processors/**`.
> Фактический объём: **320 production .py файлов / 47 451 LOC** в `src/backend/dsl/engine/processors/**` + **8 файлов / ~1 100 LOC** в `src/backend/dsl/engine/` + **5 файлов / ~1 130 LOC** в `src/backend/dsl/registry/**`. Плюс 19 YAML blueprints (`src/backend/dsl/blueprints/*.yaml`) + 4 python-blueprint + 7 macros.

---

## 0. Сводка готовности

| Подкатегория | Готовность | Обоснование |
|---|---|---|
| `@processor` декоратор + ProcessorRegistry | **40%** | 71 `@processor(...)` decorator call обнаружен в `dsl/engine/processors/`, 58 уникальных имён (один файл — `eip/resilience.py` — содержит 4 регистрации). При runtime-импорте всех основных пакетов в `ProcessorRegistry` фактически попадает **22 спецификации** (`/home/user/dev/gd_integration_tools/.venv/bin/python` — прямой замер). **~388 классов `class XxxProcessor`** найдено по `grep "^class \w+(Processor\|Task\|Router\|Adapter)"` — формально зарегистрировано **~6%**. Остальные доступны только через `import src.backend.dsl.engine.processors.xxx` или YAML-builder, но НЕ через `get_processor_registry().get("core:<name>")`. |
| `@processor` регистрация vs blueprints | **55%** | Blueprints (`*.yaml`) ссылаются на 66 уникальных step-имён (например `crud_bulk_create`, `assign_group`, `audit`, `choice`, `chunk_text`, `clip_embed`, `detect_modality`, `dlq_scan`, `es_delete`, `es_index`, `crud_upsert`, `notify_cascade`, `for_each`, `hitl_request`, `hitl_wait`, `http_call`, `llm_call`, `llm_parser`, `markitdown_extract`, `ml_inference`, `parallel`, `parse_file`). Большинство этих имён НЕ зарегистрировано в `ProcessorRegistry`. Это означает: blueprints технически декларативны (YAML), но runtime не имеет schema-валидации для их параметров, LSP/AsyncAPI не видит полный каталог, schema-export `make schemas` пишет только 22 файла из ~66 ожидаемых. |
| Blueprint patterns R2 (catalog) | **85%** | 19 YAML blueprints + 4 Python-blueprints (`_python_blueprints.py`) + 7 macros. Все blueprints возвращают `Pipeline` через `RouteBuilder` или compose-step DSL. Проверены: `api_normalize_persist_webhook`, `cdc_enrich_publish`, `file_watch_parse_validate_action`, `request_response_with_compensation`, `etl_pipeline`, `webhook_relay`, `ai_qa_pipeline`, `safe_action`, `crud_with_audit`, `scrape_and_store`, `format_bridge`, `polling_etl`. Coverage включает REST→normalize→persist, CDC→enrich→publish, file-watch→validate→action, request-response+saga. **Gap:** `webhook_relay` ссылается на `webhook_call`, но такого процессора в реестре нет. |
| `ProcessorRegistry` thread-safety + conflict-resolution | **95%** | `dsl/registry/processor.py:102-145` — `threading.Lock` на `_by_fqn`, явный `replaces=` для override. Тест `tests/unit/dsl/engine/processors/eip/test_processor_decorator_cycle38.py` (cycle-38 regression B-04 fix) фиксирует канон: 7 sample-процессоров должны быть зарегистрированы с `core:` namespace, `eip` тег, `dsl.eip.*` capability. |
| Lazy registry + capability-gate | **85%** | `dsl/registry/lazy_processor.py` — `LazyProcessorRegistry` + `LazyProcessorRef` + `lru_cache(512)` для module imports. Capabilities резолвятся без import (`capabilities_for(fqn)`). `LazyProcessorRegistry.resolve(fqn)` идемпотентно резолвит через `@processor` import-side-effect. |
| JSON-Schema export (ADR-0058) | **85%** | `dsl/registry/json_schema_exporter.py:54-122` — пишет `{fqn}.schema.json` + `index.json` для LSP/AsyncAPI. **Gap:** экспортирует только то, что реально зарегистрировано (~22 файла) — non-registered processors отсутствуют в schema-catalog. |
| Dual-mode DSL (YAML ↔ Python builder) | **70%** | `to_spec()` (`processors/base.py:144-151`, абстрактный) → `to_dict()` (`engine/pipeline.py:86-118`) → `to_yaml()` (`engine/pipeline.py:120-133`). 388 processor classes, но **~30%** реализуют `to_spec()`. Без `to_spec()` round-trip сериализация не работает — YAML → Python → YAML теряет шаги. |
| Validation (PipelineValidator) | **80%** | `engine/validation.py` (169 LOC) — проверка ordering (SanitizePII → LLMCall → RestorePII), error handling (TryCatch/Retry/DeadLetter/Fallback для external), circular refs. **Gap:** hard-coded class names (`SanitizePIIProcessor`, `RestorePIIProcessor`, `LLMCallProcessor`, `TryCatchProcessor`, `RetryProcessor`, `DeadLetterProcessor`, `FallbackChainProcessor`, `DispatchActionProcessor`, `MCPToolProcessor`, `PipelineRefProcessor`) — не extension-friendly, новые processor names не покрываются. |
| DSL Linter | **85%** | `engine/linter.py` (277 LOC) — E001 пустой pipeline, W001 dead code после stop, E002 неизвестный action, W002 PII order, I001 optimization hints, _PROPERTY_READ_PATTERNS dict. **Gap:** `_PROPERTY_READ_PATTERNS` — hard-coded dict из 14 процессоров, новые processors не покрываются. |
| Tenant-isolation invariant (K-ARCH-4) | **90%** | `engine/execution_engine.py:155-173` `_check_tenant_aware(pipeline)` — `tenant_aware=True` → must have `tenant_id` from `RequestContext` OR `current_tenant()`, raise `TenantContextRequiredError`. `ExchangeMeta.tenant_id: str \| None = None` + `ExecutionEngine.execute:279-281` propagates `tenant_id` to `properties`. **Gap:** если ни `RequestContext` ни `current_tenant()` не заданы, НЕТ check на `tenant_aware=False` path (silently допускает cross-tenant). |
| Middleware chain + timeout | **85%** | `engine/middleware.py:164-211` — `MiddlewareChain.execute()` оборачивает каждый processor через `TimeoutMiddleware` (per-processor timeout), `ErrorNormalizerMiddleware`, `MetricsMiddleware`. Per-processor `_overrides: dict[str, float]`. **Gap:** timeout пробрасывается только через `MiddlewareChain.execute(processor, ..., timeout=...)` (line 184) — `TimeoutProcessor` (`eip/resilience.py:419-463`) реализует свой `asyncio.wait_for` локально, что ведёт к double-timeout при использовании вместе с middleware. |
| `ExecutionEngine` parallel + pool | **85%** | `engine/processor_pool.py:82-303` — `ProcessorPool` с `asyncio.Semaphore(max_workers)`, thread pool для sync-processors, `_execute_one` с timeout (None=disabled, 0=disabled, >0=asyncio.wait_for). `engine/execution_engine.py:336-387` `execute_parallel` — feature_flag + tenant_aware checks before parallel. **Gap:** `shutdown()` (lines 280-295) — при `cancel_pending=True` cancel'ит все tasks, но НЕ ждёт их фактического завершения (нет `await asyncio.gather` после cancel). Best-effort cleanup. |
| OpenAPI generation | **60%** | `engine/openapi_generator.py:19-238` — генерирует OpenAPI 3.0.3 spec из Pipeline. Использует `pipeline.protocol` (REST/gRPC/WS/SSE/GraphQL). **Gap:** схемы request/response жёстко зашиты `"type": "object"` (lines 130, 159) — НЕ берёт processor-специфичные JSON-Schema из `ProcessorRegistry.export_schemas()`. OpenAPI из processors специфичны только через `x-processors` extension (line 120). |
| Dry-run emulator | **85%** | `engine/dry_run.py` (164 LOC) — `_LATENCY_PROFILE` dict с 19 процессорами (http_call, soap_call, grpc_call, call_function, db_query_external, db_call_procedure, crud_create, llm_call, ai, audit, transform, choice, parallel, validate_response, publish_event, log, etc.) — эмулирует waterfall. **Gap:** профили НЕ синхронизированы с реальным runtime latency — magic numbers без telemetry. |
| Failure-as-data (fail-loud) для processors | **70%** | `eip/resilience.py:51-179` `DeadLetterProcessor` — 3-stage DLQ (Redis → JSONL → metric+raise). **Exemplary:** ни silent loss, ни suppress exceptions. **Gap:** НЕ все external-процессоры оборачиваются в `DeadLetterProcessor` — `InvokeProcessor` (`processors/invoke.py:131-164`) обрабатывает ERROR через `exchange.stop()` без DLQ-pipeline. |
| Auth-coupling на уровне processor | **70%** | `processors/base.py:73-135` `BaseProcessor.auth_check()` — capability-gate через `check_source_capability`. 12+ процессоров используют `required_capability: ClassVar[str \| None]` + `audit_event: str \| None` (e.g. `FileListProcessor.rpa.file.list`, `FileWatchProcessor.fs.watch`, `ShellExecProcessor.rpa.shell.exec`, `SkillInvokeProcessor.skill.invoke`, `MCPToolProcessor.mcp.tool.invoke`, `AgentGraphProcessor.agent.graph.invoke`). **Gap:** `processors/core.py` `DispatchActionProcessor` (lines 80-122) — главный процессор DSL — НЕ имеет `required_capability` → DSL pipeline с `dispatch_action` НЕ capability-gated на processor-уровне (полагается на action-handler). |
| `BaseAIProcessor` (`processors/agent_dsl/_base.py`) — exemplary pattern | **90%** | 80 LOC template-method: feature_flag → capability → `_run` → audit. Используется в 10 Agent DSL processors. **Gap:** `_run` (lines 78-86) бросает `NotImplementedError` если subclass не override — это правильное abstract-method, но без `# pragma: no cover` (в отличие от `processors/base.py:142`). Marker-traceability страдает. |
| Tracing & observability | **85%** | `engine/tracer.py` (213 LOC) — `ExecutionTracer.trace()` context manager, SSE subscribers, in-memory + pluggable `TraceStorage`. `_TRACE_BUFFER_MAXLEN=1000` ring buffer. **Gap:** `_storage` (`InMemoryTraceStorage`) — per-process, на multi-pod deployment не собирается. Persistent storage = TD-026 (S45+ D — backlog). |

**ИТОГОВАЯ ОЦЕНКА: 65%**

Обоснование: Архитектура engine'а и registry'а sound (`ProcessorRegistry` thread-safe, lazy resolution, capability-gate, JSON-Schema export), Blueprint R2 catalog широкий (19 YAML + 4 Python + 7 macros), `ExecutionEngine` + `MiddlewareChain` + `ProcessorPool` корректно реализуют async-first pipeline. **Главные подрывы**: (а) **~6% processor-классов зарегистрированы через `@processor` декоратор** — `get_processor_registry().list_specs()` = 22 вместо ожидаемых ~270; (б) **9+ классов дублируются** между файлами с одинаковыми именами (MCPToolProcessor, FileWatchProcessor, AgentGraphProcessor, SamplingProcessor, ScreenshotProcessor, ClickProcessor, NavigateProcessor, ExtractProcessor, _BankingAIProcessor); (в) **legacy `eip/reliability.py` 442 LOC** — DEAD CODE shadowed by `eip/reliability/__init__.py` package; (г) **legacy `eip/flow_control.py` отсутствует**, но `flow_control/_legacy.py` остаётся 1-line stub (acceptable, не проблема); (д) **`security.py` импортирует из shim** `entrypoints.api.dependencies.auth_selector` — `_VERIFIERS` удалён из shim в S162 W5, DSL всегда fails-closed с `AuthenticationProviderUnavailableError` (P0 blocking); (е) **`external.py` 139 LOC** — содержит дубликаты `MCPToolProcessor` и `AgentGraphProcessor`, shadowed by `agent_dsl/*.py` через `__init__.py`.

---

## 1. Таблица находок

| ID | Приоритет | Файл:строка | Описание | Предложенный фикс | Экономия строк | Доказательство |
|----|-----------|-------------|----------|-------------------|----------------|-----------------|
| **D-A7-01** (P0) | КРИТ | `src/backend/dsl/engine/processors/security.py:52` | **Auth DSL fails-closed ВСЕГДА** — `_VERIFIERS_MODULE = "src.backend.entrypoints.api.dependencies.auth_selector"`, но `_VERIFIERS` УДАЛЁН из shim'а в S162 W5 (`entrypoints/api/dependencies/auth_selector.py:31` комментарий: `removed _VERIFIERS from re-exports — private symbol`). Канонический путь теперь `core/auth/auth_selector.py:214-222`. DSL `_load_verifiers()` всегда бросает `AuthenticationProviderUnavailableError("verifier registry attribute missing in ...")` — production breaker: ВСЕ DSL pipelines с `AuthValidateProcessor` НЕ работают. Тест `tests/unit/dsl/processors/security/test_auth_validate_failclosed.py` фиксирует именно этот bug как expected behavior (D-AUDIT-03 cycle-2 fix), что маскирует regression. | Заменить `_VERIFIERS_MODULE = "src.backend.core.auth.auth_selector"` (canonical). Убрать assertion о missing attribute — теперь это success path. Тест переписать на позитивный: `_load_verifiers()` возвращает dict из 7 AuthMethod. | −8 LOC (cleanup) | Прямая цитата `_VERIFIERS_MODULE = "src.backend.entrypoints.api.dependencies.auth_selector"` + `core/auth/auth_selector.py:214` определяет `_VERIFIERS` + `entrypoints/api/dependencies/auth_selector.py:31` явно говорит "removed _VERIFIERS". Runtime-проверка через `.venv/bin/python`: `from src.backend.entrypoints.api.dependencies.auth_selector import _VERIFIERS` → AttributeError. |
| **D-A7-02** (P0) | КРИТ | `src/backend/dsl/engine/processors/external.py:7-71` | **DEAD duplicates: `MCPToolProcessor` + `AgentGraphProcessor`** — `external.py` определяет оба класса (BaseProcessor-based, simple), но `__init__.py:8-9` импортирует ТОЛЬКО из `agent_dsl/mcp_tool.py` и `agent_dsl/agent_graph.py` (BaseAIProcessor-based, secure). Классы из `external.py` НИКОГДА не импортируются, только register в `__all__`. При попытке `from src.backend.dsl.engine.processors.external import MCPToolProcessor` получим simple-version БЕЗ file:// transport deny (`agent_dsl/mcp_tool.py:88`). **Shadowed attack surface** — менее secure версия существует в кодовой базе. | Удалить `MCPToolProcessor` и `AgentGraphProcessor` из `external.py`, оставить только `CDCProcessor`. `__all__` = `("CDCProcessor",)`. | −60 LOC | `grep -rn "from src.backend.dsl.engine.processors.external import" src/ --include="*.py"` → 0 hits. `external.py:7` `__all__ = ("AgentGraphProcessor", "CDCProcessor", "MCPToolProcessor")`. `__init__.py:8` импортирует ТОЛЬКО `agent_dsl.mcp_tool.MCPToolProcessor`. |
| **D-A7-03** (P1) | ВЫС | `src/backend/dsl/engine/processors/file_watch.py` (304 LOC) + `src/backend/dsl/engine/processors/rpa/operations/filewatchprocessor.py` (120 LOC) | **Дубль имени `FileWatchProcessor` с разной семантикой** — `file_watch.py:41` регистрирует `@processor("file_watch", namespace="core", ...)` (one-shot glob scan, S176 #4 multi-pattern). `rpa/operations/filewatchprocessor.py:47` определяет класс БЕЗ `@processor` decorator (watchdog Observer, real-time). Оба класса называются `FileWatchProcessor`, но второй импортируется только через `rpa/operations/__init__.py` re-export. **Naming collision + разная семантика** (scan vs watch). Builder `file_sources_mixin.py:114-121` резолвит `"FileWatchProcessor"` через `_add_lazy("src.backend.dsl.engine.processors.file_watch", ...)` — hardcoded к one-shot. Если плагин хочет real-time watch, получит не то. | Переименовать `rpa/operations/filewatchprocessor.py:FileWatchProcessor` → `RpaFileWatchProcessor` (или `DirectoryObserverProcessor`). Синхронизировать с D-A7-09 (FilteredDirectoryScanProcessor). | −10 LOC (cleanup) | Прямая проверка: `grep -E "^class FileWatchProcessor" src/backend/dsl/engine/processors/ -r --include="*.py"` → 2 hits (`file_watch.py:41`, `rpa/operations/filewatchprocessor.py:47`). |
| **D-A7-04** (P1) | ВЫС | `src/backend/dsl/engine/processors/eip/reliability.py` (442 LOC) | **DEAD legacy god-file** — `eip/reliability.py` 442 LOC содержит 4 класса (CorrelationIdentifierProcessor, MessageExpirationProcessor, RedeliveryPolicyProcessor, ReturnAddressProcessor). Новый split `eip/reliability/{correlation_identifier.py, message_expiration.py, redelivery_policy.py, return_address.py, _legacy.py}` (S175 Phase 2) shadowed'ит legacy. Но legacy `.py` ФАЙЛ всё ещё импортируется через `eip/__init__.py:64` `from src.backend.dsl.engine.processors.eip.reliability import (...)`. Runtime `.venv/bin/python` подтверждает: импорт резолвится в `reliability/__init__.py` (package), НЕ legacy. Файл 442 LOC — dead code, не компилируется, но хранится. | Удалить `eip/reliability.py` (442 LOC). Проверить, что `eip/__init__.py:64` импорт продолжает работать через package. | −442 LOC | Runtime check: `/home/user/dev/gd_integration_tools/.venv/bin/python -c "from src.backend.dsl.engine.processors.eip import reliability as r; print(r.__file__)"` → `/home/user/dev/gd_integration_tools/src/backend/dsl/engine/processors/eip/reliability/__init__.py`. Прямая проверка: `eip/__init__.py:64` импортирует с того же `from` path, что и до split — Python резолвит в `__init__.py` потому что он package directory (а не `.py`). |
| **D-A7-05** (P1) | ВЫС | `src/backend/dsl/engine/processors/web.py` (167 LOC) + `src/backend/dsl/engine/processors/rpa_browser.py` (466 LOC) | **Дубликаты Playwright-процессоров** — оба файла определяют `NavigateProcessor`, `ClickProcessor`, `ExtractProcessor`, `ScreenshotProcessor`. `web.py:34` использует `services.io.web_automation.get_web_automation_service()` (другой backend), `rpa_browser.py:97` использует `PlaywrightBrowserPool`. Семантика частично совпадает (navigate/click/extract), частично различается. Naming collision в `__init__.py:8-13` (`from ...processors.web import ...`) vs `from ...processors.rpa_browser import ...` — но `__init__.py` импортирует только из `rpa_browser`. `web.py` — dead code. | Удалить `web.py`. Перенести `FillFormProcessor`, `RunScenarioProcessor` (web.py-only) в `rpa_browser.py` если нужны. | −100 LOC | `grep -rn "from src.backend.dsl.engine.processors.web import" src/ --include="*.py"` → 0 hits. `__init__.py` импортирует только из `rpa_browser`. `web.py:7-16` `__all__` содержит 6 классов, все они перекрыты `rpa_browser.py`. |
| **D-A7-06** (P1) | ВЫС | `src/backend/dsl/engine/processors/eip/filter_router_sampling.py` (SamplingProcessor) + `src/backend/dsl/engine/processors/streaming/operations.py` (SamplingProcessor) | **Дубль имени `SamplingProcessor`** — оба файла определяют класс с одинаковым именем. `eip/filter_router_sampling.py:25` — "probabilistic subset: пропускает N-ую часть messages" (rate-based). `streaming/operations.py` — "вероятностный сэмплинг — пропускает сообщение с вероятностью `probability`" (probability-based). Разная семантика, но один name. Ни один не зарегистрирован через `@processor` — оба импортируются ad-hoc. | Переименовать streaming version в `ProbabilitySamplingProcessor`, оставить `SamplingProcessor` в `eip/`. | −5 LOC | `grep -E "^class SamplingProcessor" src/backend/dsl/engine/processors/ -r --include="*.py"` → 2 hits. |
| **D-A7-07** (P1) | ВЫС | `src/backend/dsl/engine/processors/ai_banking/_base.py` (_BankingAIProcessor) + `src/backend/dsl/engine/processors/ai/banking_processors/base.py` (_BankingAIProcessor) | **Дубль internal helper `_BankingAIProcessor`** — оба файла определяют private base class. `ai_banking/_base.py` импортируется через `__init__.py` пакета, `ai/banking_processors/base.py` — отдельный namespace. | Удалить `ai/banking_processors/base.py` (если `ai_banking/_base.py` — canonical) или объединить через `TYPE_CHECKING` блок. | −50 LOC | Прямая проверка `grep -E "^class _BankingAIProcessor"`. |
| **D-A7-08** (P1) | ВЫС | `src/backend/dsl/engine/processors/` (388 классов vs 58 регистраций) | **~94% processor-классов НЕ зарегистрированы через `@processor`** — runtime замер `.venv/bin/python` после явных импортов всех major packages: `len(registry.list_specs()) = 22`. Из 71 `@processor(...)` decorator call многие — несколько регистраций в одном файле (`eip/resilience.py` имеет 4). Реально уникальных зарегистрированных имён: **22** в `core` namespace. Это значит: schema-export пишет 22 файла, LSP видит 22 процессора, blueprints ссылаются на 66 step-имён, из которых ~44 НЕ зарегистрированы. **Это блокирует D-A7-09 (канонический реестр для расширений)**. | Цикл migrate-remaining: `@processor(...)` decorator для всех ~388 классов с namespace (`core:` / `<plugin>:`). Приоритет: blueprints (66 step-имён) → DSL core (dispatch_action, transform, enrich, filter, validate, log, set_header, set_property) → AI (llm_call, rag_query, sanitize_pii, restore_pii, etc.) → EIP остаток (35 не зарегистрированных). Оценка effort: 2-3 sprints цикл по ~40 регистраций/спринт. | +388 LOC (deco additions), но −2500+ LOC YAGNI-elimination (если соптимизировать naming/structure) | `python3 -c "from src.backend.dsl.registry.processor import get_processor_registry; ..."` (см. ниже). `grep -c "^class.*Processor" --include="*.py" -r src/backend/dsl/engine/processors/` → 388. `grep -c "@processor(" -r src/backend/dsl/engine/processors/ --include="*.py"` → 71 (58 unique). |
| **D-A7-09** (P1) | ВЫС | `src/backend/dsl/engine/processors/fs_directory_scan.py` (247 LOC) + `src/backend/dsl/engine/processors/rpa/operations/filtereddirectoryscanprocessor.py` (130 LOC) | **DEPRECATED shim vs canonical** — `fs_directory_scan.py:63` `DirectoryScanProcessor` помечен DEPRECATED (S172 M1.2), эмитит `DeprecationWarning` при каждом `__init__`. Делегирует в `FilteredDirectoryScanProcessor` (S171 M7, async-safe, asyncio.to_thread + asyncio.wait_for + size/mtime filters + safety caps). **Правильный паттерн.** Однако `_DeprecationAuditEmitted._emitted: bool` (lines 53-60) — per-process guard через class-attribute mutation (not thread-safe, но для audit-flag ОК). Реальные риски: (а) shim всё ещё перехватывает `set_result` через closure (lines 189-204) — fragile monkey-patching; (б) 247 LOC shim с legacy result format; (в) S172 план на удаление в Sprint 175 — пора. | Удалить `fs_directory_scan.py` целиком. Проверить, что shim больше не используется. | −247 LOC | `grep -rn "fs_directory_scan\|DirectoryScanProcessor\b" src/ --include="*.py"` → только `__init__.py` exports. |
| **D-A7-10** (P2) | СРЕД | `src/backend/dsl/engine/processors/__init__.py:343` (`"MCPToolProcessor"`) | **`MCPToolProcessor` alias-collision risk** — `__init__.py:8` импортирует `from src.backend.dsl.engine.processors.agent_dsl.mcp_tool import MCPToolProcessor` (BaseAIProcessor-based, secure). `external.py:10` тоже определяет `class MCPToolProcessor(BaseProcessor)` (simple). Python resolves `from src.backend.dsl.engine.processors import MCPToolProcessor` через `__init__.py:343` — `agent_dsl` версия. Но `from src.backend.dsl.engine.processors.external import MCPToolProcessor` → simple version. **Два пути импорта → два класса → разная семантика** (security-relevant: simple version НЕ проверяет `file://` transport deny). | В D-A7-02 (выше). Дополнительно: добавить `linter.py` warning "import from .external для MCPToolProcessor" → suggest canonical path. | 0 | Прямая проверка `__init__.py:8` vs `external.py:10`. |
| **D-A7-11** (P2) | СРЕД | `src/backend/dsl/engine/processors/agent_dsl/skill_invoke.py:96-101` | **`SkillInvokeProcessor` silent pass-through при `NotImplementedError`** — `except NotImplementedError: _logger.warning("%s: SkillRegistry.invoke() — scaffold (NotImplementedError) — skip", ...)` → return без записи в exchange. Skill не вызван, но pipeline продолжается. В production это означает: missing skill НЕ блокирует pipeline (silent no-op). Docstring (lines 1-21) explicitly says "silent pass-through" — это **by design**, но безопаснее fail-closed. | Добавить `settings.skills.fail_on_unavailable = False` (default False для backward compat). При True — `exchange.set_error("skill_unavailable")` + `exchange.stop()`. | +5 LOC | Прямая цитата: `except NotImplementedError: _logger.warning(...); return`. |
| **D-A7-12** (P2) | СРЕД | `src/backend/dsl/engine/processors/streaming_llm_publishers.py:22, 26` | **`raise NotImplementedError` в abstract `_BasePublisher`** — правильный abstract-method pattern. **Acceptable.** Маркирован как legitimate abstract (не bug). | — (no fix, exemplary) | — | Прямая проверка: `_BasePublisher` — `ABC`-like base class без `@abstractmethod`, но семантически abstract. Docstring: "Abstract: publish LLM token chunk". |
| **D-A7-13** (P2) | СРЕД | `src/backend/dsl/engine/execution_engine.py:76-124` + `engine/execution_engine.py:111-124` `_cached_validate` | **`_validation_cache` unbounded growth** — `self._validation_cache: dict[tuple[str, tuple[str, ...]], Any]` НЕ имеет TTL/MAX_SIZE. При росте pipeline count (multi-tenant × route variants × hot-reload) cache может расти unbounded. `invalidate_validation_cache` (lines 126-138) — единственный cleanup path. | Добавить `@lru_cache(maxsize=512)` decorator или `OrderedDict` с TTL eviction. | +5 LOC | Прямая цитата: `self._validation_cache: dict[tuple[str, tuple[str, ...]], Any] = {}` (no maxsize). |
| **D-A7-14** (P2) | СРЕД | `src/backend/dsl/engine/processors/invoke.py:131-164` | **`InvokeProcessor` ERROR path → silent exchange.stop без DLQ** — при `InvocationStatus.ERROR`, `InvokeProcessor.process()` вызывает `exchange.stop()` и записывает error в `result_property`, но НЕ регистрирует event в DLQ pipeline. `DeadLetterProcessor` (`eip/resilience.py:51-179`) применяется как wrapper на pipeline, но `InvokeProcessor` сам по себе НЕ оборачивает error в DLQ event. Разница: wrapped pipeline с DLQ → dead-letter entry; standalone InvokeProcessor → только exchange.stop. | Внутри `InvokeProcessor.process()` при ERROR вызвать `DeadLetterProcessor._send_to_dlq(exchange)` если DLQ-configured. Или emit `invocation.error` metric. | +10 LOC | Прямая цитата: `case InvocationStatus.ERROR: exchange.set_property(...); exchange.stop()`. |
| **D-A7-15** (P2) | СРЕД | `src/backend/dsl/engine/processors/proxy/expose.py` + `processors/proxy/forward.py` + `processors/proxy/headers.py` + `processors/proxy/redirect.py` | **4 proxy-процессора без `@processor` регистрации** — все 4 файла импортируются через `__init__.py`, но НЕ зарегистрированы в `ProcessorRegistry`. Не входят в schema-export, blueprints не могут reference them по имени (только через `process(XxxProcessor(...))`). | Регистрация `@processor("proxy_expose", namespace="core", ...)`, etc. | +30 LOC | `grep -E "@processor\(" src/backend/dsl/engine/processors/proxy/` → 0 hits. |
| **D-A7-16** (P2) | СРЕД | `src/backend/dsl/engine/processors/express/` (edit.py, mention.py, reply.py, send.py, send_file.py, status.py, typing.py, _common.py) | **8 Express-like процессоров без регистрации** — модуль экспортирует 7 публичных классов через `__init__.py`, но ни один не зарегистрирован. Не попадают в schema-export. | Регистрация `@processor("express_send", ...)`, etc. | +40 LOC | `grep -E "@processor\(" src/backend/dsl/engine/processors/express/` → 0 hits. |
| **D-A7-17** (P2) | СРЕД | `src/backend/dsl/engine/processors/agent_dsl/agent_graph.py` + `processors/agent_dsl/agent_loop.py` + `processors/agent_dsl/agent_parallel.py` + `processors/agent_dsl/agent_branch.py` + `processors/agent_dsl/langgraph_agent.py` + `processors/agent_dsl/plan_execute.py` + `processors/agent_dsl/reflection_loop.py` (7 файлов) | **7 Agent DSL процессоров без регистрации** — все наследуют `BaseAIProcessor`, определены в `agent_dsl/`, но НЕ зарегистрированы через `@processor`. Blueprint `agent_graph` ссылается на них по имени, но schema-export их не видит. | Регистрация `@processor("agent_graph", namespace="ai", ...)`, etc. | +35 LOC | `grep -E "@processor\(" src/backend/dsl/engine/processors/agent_dsl/` → 1 hit (`optimize_prompt.py`). |
| **D-A7-18** (P2) | СРЕД | `src/backend/dsl/engine/processors/sink_publish/` + `processors/notify/` + `processors/telegram/` + `processors/streaming/` + `processors/storage/` + `processors/control_flow/` + `processors/components/` + `processors/agent_dsl/` | **~15 sub-packages без регистрации** — все имеют `__init__.py` с re-export, но без `@processor` декораторов. Storage (`s3.py` зарегистрировал 5 операций), control_flow (Choice, Saga, Parallel), components (HttpCall, FileRead, DatabaseQuery), sink_publish, notify (apprise), telegram (Telegram bot), streaming (windows/operations/reliability) — все unregistered. | Цикл регистрации: storage+control_flow+components (high priority, часто используются). | +120 LOC | `grep -c "@processor(" src/backend/dsl/engine/processors/{sink_publish,notify,telegram,streaming,storage,control_flow,components,agent_dsl}/*/*.py` → всего ~5 hits из ~80 классов. |
| **D-A7-19** (P2) | СРЕД | `src/backend/dsl/engine/processors/integration_send.py` + `integration.py` | **`integration.py` (EventPublish/MemoryLoad/MemorySave) vs `integration_send.py` — naming confusion** — оба файла в `processors/`, оба определяют processors для messaging. Без общего `__init__.py` re-export ordering, IDE autocomplete путается. | Объединить в `messaging/` package или явно разделить concerns (events vs memory). | 0 (refactor) | Прямая проверка `ls src/backend/dsl/engine/processors/integration*.py`. |
| **D-A7-20** (P2) | СРЕД | `src/backend/dsl/engine/openapi_generator.py:130, 159` | **Hardcoded `"type": "object"` в OpenAPI request/response schemas** — НЕ использует `ProcessorRegistry.export_schemas()` для генерации processor-specific schemas. OpenAPI spec имеет generic shape для ВСЕХ процессоров. | Использовать `registry.export_schemas()` для `components.schemas`. | +15 LOC | Прямая цитата: `"type": "object"` в request/response без processor-specific schema reference. |
| **D-A7-21** (P2) | СРЕД | `src/backend/dsl/engine/linter.py:34-49` `_PROPERTY_READ_PATTERNS` | **Hardcoded dict на 14 процессоров** — новые процессоры (например, новые AI processors) не покрываются linter'ом для property-usage detection. Maintenance burden: при добавлении нового процессора нужно вручную добавлять entry. | Сделать data-driven: процессоры декларируют `reads_properties: list[str]` через class attribute или `@processor(...)` meta-field. | −10 LOC (упрощение) | Прямая проверка `_PROPERTY_READ_PATTERNS`. |
| **D-A7-22** (P2) | СРЕД | `src/backend/dsl/engine/validation.py:74-141` | **Hardcoded class names** — `SanitizePIIProcessor`, `RestorePIIProcessor`, `LLMCallProcessor`, `PromptComposerProcessor`, `TryCatchProcessor`, `RetryProcessor`, `DeadLetterProcessor`, `FallbackChainProcessor`, `DispatchActionProcessor`, `MCPToolProcessor`, `PipelineRefProcessor`. Расширения с custom processor НЕ покрываются. | Использовать `ProcessorRegistry.list_by_tag("p11_safe")` или class attribute `validates_ordering: list[str]` через base class. | −20 LOC (refactor) | Прямая проверка: 11 hardcoded class names в validation.py. |
| **D-A7-23** (P2) | СРЕД | `src/backend/dsl/engine/processors/eip/marshal/processors.py:21` + `eip/marshal/base.py:20` + `eip/marshal/formats.py:24` | **defusedxml fallback** — `try: import defusedxml.ElementTree as DET except ImportError: DET = None`. В dev-light (без defusedxml) marshal XML path silently falls back на `xml.etree.ElementTree` — UNSAFE для untrusted input. Без security audit какой mode активен. | Raise на отсутствии defusedxml в production (`settings.xml.marshal.require_defusedxml = True` default). | +5 LOC | Прямая цитата: `except ImportError:  # pragma: no cover — dev-light fallback: DET = None`. |
| **D-A7-24** (P2) | СРЕД | `src/backend/dsl/engine/processors/eip/resilience.py:419-463` `TimeoutProcessor` | **Double-timeout risk с MiddlewareChain** — `TimeoutProcessor` оборачивает в `asyncio.wait_for(timeout=self._seconds)` (line 452), но `MiddlewareChain.execute()` УЖЕ оборачивает в `asyncio.wait_for(..., timeout=timeout)` если задан. Двойная защита может привести к `TimeoutError` от inner-обёртки, что не отличимо от processor failure. | Либо убрать timeout в `TimeoutProcessor.process()` (положиться на middleware), либо добавить `settings.timeout.disable_middleware_double = True` и явно документировать. | −5 LOC | Прямая проверка `eip/resilience.py:452` (`asyncio.wait_for`) vs `engine/middleware.py:193-198` (`asyncio.wait_for`). |
| **D-A7-25** (P3) | НИЗК | `src/backend/dsl/engine/processor_pool.py:280-295` `shutdown()` | **`shutdown()` cancel pending без await** — `for task in self._active: task.cancel()` → `await asyncio.gather(*self._active, return_exceptions=True)` (line 291) ждёт, но `cancel_pending=True` без graceful timeout — может зависнуть на долгих I/O. | Добавить timeout: `await asyncio.wait_for(asyncio.gather(...), timeout=5.0)` (graceful), затем force-cancel остальные. | +5 LOC | Прямая цитата: `if cancel_pending: for task in self._active: if not task.done(): task.cancel(); await asyncio.gather(*self._active, return_exceptions=True)` — no timeout. |
| **D-A7-26** (P3) | НИЗК | `src/backend/dsl/engine/processor_pool.py:152-160` | **Sync-processor path uses `loop.run_in_executor`** — для НЕ-async `processor.process()`. Стандартный паттерн, но `asyncio.get_event_loop()` deprecated в Python 3.14+, рекомендуется `asyncio.get_running_loop()`. | Заменить `asyncio.get_event_loop()` на `asyncio.get_running_loop()`. | 0 (refactor) | Прямая цитата: `loop = asyncio.get_event_loop()`. |
| **D-A7-27** (P3) | НИЗК | `src/backend/dsl/engine/processors/db_crud.py:49-52` `SUPPORTED_DIALECTS` | **Hardcoded dialect set** — `frozenset({"postgresql", "sqlite", "mysql", "oracle", "mssql"})`. Расширения с ClickHouse, DB2, или InfluxDB должны patch через monkey-patching. | Зарегистрировать dialects через `ProcessorRegistry.meta` или `core/dialects/registry.py`. | +10 LOC | Прямая цитата. |
| **D-A7-28** (P3) | НИЗК | `src/backend/dsl/engine/processors/eip/reliability/_legacy.py:61-87` | **Manual `__getattr__` for backward-compat** — `def __getattr__(name)` resolves 4 processor classes lazy. Pattern correct (PEP 562), но использует 4 if-statements вместо dict-mapping. При добавлении нового processor'а в split — нужно вручную добавить if-statement в `_legacy.py`. | Dict-mapping: `_LAZY_EXPORTS = {"CorrelationIdentifierProcessor": (...)}`. | 0 (refactor) | Прямая проверка 4 if-statements в `_legacy.py`. |
| **D-A7-29** (P3) | НИЗК | `src/backend/dsl/engine/processors/agent_dsl/_base.py:86` | **`raise NotImplementedError` без `# pragma: no cover`** — в отличие от `processors/base.py:142` (с pragma), abstract `_run` не помечен. Coverage tool может flag'нуть как untested branch. | Добавить `# pragma: no cover`. | 0 | Прямая цитата: `raise NotImplementedError(f"{type(self).__name__} must override _run(exchange, context)")`. |
| **D-A7-30** (P3) | НИЗК | `src/backend/dsl/engine/processors/eip/transformation.py:449` (макс размер) | **449 LOC god-file** — `eip/transformation.py` содержит 4 processor classes (Splitter, Sort, MessageTranslator, Normalizer, ClaimCheck) — should be split per processor (как reliability/). | Split по аналогии с reliability: `eip/transformation/{splitter.py, sort.py, message_translator.py, normalizer.py, claim_check.py}`. | −449 LOC (после split) +5×20 LOC = −350 LOC | `wc -l src/backend/dsl/engine/processors/eip/transformation.py` → 449. |
| **D-A7-31** (P3) | НИЗК | `src/backend/dsl/engine/processors/eip/windowed_dedup.py:384` (макс размер) | **384 LOC god-file** — содержит WindowedDedupProcessor, WindowedCollectProcessor, BatchWindowProcessor, TumblingWindowProcessor, SessionWindowProcessor, SlidingWindowProcessor. | Split по аналогии: `eip/streaming/{windowed_dedup.py, windowed_collect.py, batch_window.py, ...}`. | −384 LOC + 7×25 = −200 LOC | Прямая проверка `wc -l`. |
| **D-A7-32** (P3) | НИЗК | `src/backend/dsl/engine/processors/eip/api_composition.py:380` | **380 LOC god-file** — содержит ApiCompositionProcessor + 4-5 helpers. | Split: `eip/api_composition/{processor.py, helpers.py}`. | −250 LOC | Прямая проверка. |
| **D-A7-33** (P3) | НИЗК | `src/backend/dsl/engine/processors/agent_dsl/agent_graph.py:423` + `agent_dsl/ai_tool_dispatch.py:405` | **2 god-files (>400 LOC каждый)** — `agent_graph.py` (423 LOC) — AgentGraphProcessor + LangGraph integration + state machine. `ai_tool_dispatch.py` (405 LOC) — AIToolDispatch + tool registry + audit. | Split per-processor + helper extraction. | −400 LOC | Прямая проверка. |
| **D-A7-34** (P3) | НИЗК | `src/backend/dsl/engine/processors/db_crud.py:411` | **411 LOC god-file** — DbCrudProcessor + 5 helper functions (build_insert_sql, build_update_sql, build_upsert_sql + 3 dialect-specific variants, build_delete_sql). 45 LOC __all__. | Split helpers в отдельный модуль. | −100 LOC | Прямая проверка. |
| **D-A7-35** (P3) | НИЗК | `src/backend/dsl/engine/processors/rpa_browser.py:466` | **466 LOC god-file** — 8 Playwright processors (BrowserLaunch, Navigate, Click, Fill, Extract, WaitFor, Screenshot, Pdf). Largest single processor file. | Split per-processor: `rpa/operations/{browser_launch.py, navigate.py, click.py, ...}` (mirror pattern). | −350 LOC | Прямая проверка. |
| **D-A7-36** | INFO | `src/backend/dsl/engine/processors/eip/resilience.py:51-179` `DeadLetterProcessor` | **Exemplary DLQ pattern** — 3-stage fallback (Redis → JSONL → metric+raise), никогда silent loss. Production-ready. | — (no fix, exemplary) | — | Прямая проверка 3-stage `_send_to_dlq`. |
| **D-A7-37** | INFO | `src/backend/dsl/engine/processors/agent_dsl/_base.py` (80 LOC) | **Exemplary Template Method** — feature_flag → capability → `_run` → audit. Устраняет boilerplate в 10 Agent DSL процессорах. | — (no fix, exemplary) | — | Прямая проверка `process()` template method. |
| **D-A7-38** | INFO | `src/backend/dsl/engine/processors/eip/flow_control/throttler.py:49-86` `ThrottlerProcessor` | **Exemplary token-bucket implementation** — async-safe через `asyncio.Lock`, корректный burst + rate, full @processor registration с schema/capability/tags. | — (no fix, exemplary) | — | Прямая проверка `process()` + `@processor` decorator. |
| **D-A7-39** | INFO | `src/backend/dsl/registry/processor.py:300-368` | **Exemplary @processor декоратор** — auto-detect Pydantic model via `cls.model`, atomic registration, conflict detection with `replaces=`, JSON-Schema metadata. | — (no fix, exemplary) | — | Прямая проверка `processor()` decorator. |
| **D-A7-40** | INFO | `src/backend/dsl/engine/processors/eip/resilience.py:296-397` `CircuitBreakerProcessor` | **Exemplary CB implementation** — delegates to shared `breaker_registry` (purgatory-based), DSL-namespace ``dsl.pipeline.<route_id>``, no local state. | — (no fix, exemplary) | — | Прямая проверка `_resolve_breaker_name` + `breaker.guard()`. |
| **D-A7-41** | INFO | `src/backend/dsl/engine/processors/function_call.py:24-122` `CallFunctionProcessor` | **Exemplary secure call_function** — module whitelist через `validate_module_whitelist`, capability `function.call.*`, audit-event, regex pattern для `ref` validation. | — (no fix, exemplary) | — | Прямая проверка decorator + whitelist. |

---

## 2. Явный список «не проверено» с обоснованием

| Что | Почему не проверено |
|---|---|
| **`src/backend/dsl/route/**`, `src/backend/dsl/workflow/**`, `src/backend/dsl/service/**`, `src/backend/dsl/contracts/**`, `src/backend/dsl/blueprints/**` (builders, не YAML)** | Эти области относятся к домену **A6-DSL-Route-Workflow-Service**, не A7. Проверены только результаты их использования (builders ссылаются на зарегистрированные processors; 22 specs зарегистрированы). Cross-domain validation зависит от отчёта A6. |
| **`extensions/<name>/**` DSL routes** | Относятся к домену **A10-Business-Logic-Extensions-Routes**. Проверены только `routes/hello_route`, `routes/test_route_w1` (в directory listing) — содержимое НЕ читал. |
| **`src/backend/dsl/adapters/**` (transport adapters, ProtocolType, TransportConfig)** | Используется в `engine/pipeline.py:8` и `engine/openapi_generator.py:11`, но это другой concern (multi-protocol auto-registration) — covered в A4-Entrypoints. |
| **`src/backend/dsl/templates_library.py`** (381 LOC) | Templates vs blueprints — отдельная тема, формально не в scope A7 (A7 = engine + processors + registry). Templates читают RouteBuilder, не processor registry. |
| **`src/backend/dsl/commands/registry.py` (RouteRegistry)** | Упомянут в A7-context (`engine/execution_engine.py` использует для `route_registry.get(route_id)` в Multicast), но НЕ детально проверен — это domain A6. |
| **`src/backend/dsl/cli/`, `src/backend/dsl/yaml_watcher/`** | CLI-инструменты и hot-reload watcher для YAML. Не в engine — covered в A12-Config-Environment-Ops. |
| **DSL processor test coverage details** | Проверены ТОЛЬКО: `tests/unit/dsl/engine/processors/eip/test_processor_decorator_cycle38.py` (7 sample tests), `test_redelivery_policy.py` (1 файл в `eip/reliability/`), `test_multicast.py` (1 файл в `eip/routing/`), `test_auth_validate_failclosed.py`. Полная картина coverage — задача отдельного отчёта. |
| **`src/backend/dsl/blueprints/macros/*.py`** | Прочитаны названия файлов и docstring `__init__.py`, но содержимое функций (etl_pipeline, webhook_relay, etc.) НЕ детально проверено. |
| **Plugin integration test** — как `extensions/*` регистрируют processors через `@processor` | Не проверено — domain A10. |
| **Сравнение с Camel/Airflow/LangGraph/DSPy эквивалентами** | Не проводил — задача требует external research + сравнения API surface. Quick check: есть Temporal AdaptiveConcurrency? — НЕ найден в коде (есть `ProcessorPool` semaphore-based, но НЕ adaptive). |
| **DSPy MIPROv2, RLM-toolkit** | НЕ использованы в коде (grep не находит). Это разрыв vs "эталонная" функциональность, но формально не в scope A7. |
| **SBA (Service-Based Architecture) compliance** | Не проверял — SBA относится к architectural layer. |
| **`make check-docstrings` gate (954 → 0 missing)** | Подтверждено AGENTS.md baseline, но прогон `make check-docstrings` НЕ выполнял (требует uv environment setup). |

---

## 3. Запросы к смежным доменам

| Запрос | К какому домену | Обоснование |
|---|---|---|
| **A2-Security**: проверить, что `_VERIFIERS` доступ через `core/auth/auth_selector.py:214` корректно работает для DSL через `importlib`. D-A7-01 фикс меняет import-path. | A2 | D-A7-01 fix требует подтверждения, что canonical path не нарушает auth invariants в entrypoints. |
| **A6-DSL-Route-Workflow-Service**: проверить, что RouteBuilder / WorkflowBuilder / ServiceDSL не используют unregistered processors напрямую. | A6 | Cross-domain: builders могут использовать processors ad-hoc без регистрации (валидно для SDK, невалидно для LSP/AsyncAPI). |
| **A10-Business-Logic-Extensions-Routes**: проверить, что plugin extensions регистрируют processors через `@processor` decorator vs raw registration (`register_class`). | A10 | D-A7-08 migration требует, чтобы plugin TOML schema support `@processor` decorator. |
| **A1-Infrastructure**: проверить, что `ConnectionReuseManager` для DB queries из `processors/db_crud.py` и `processors/db_query_external.py` использует canonical `core.utils.connection_reuse`, не raw connections. | A1 | DSL processors для DB не должны bypass connection lifecycle. |
| **A4-Entrypoints**: проверить, что `entrypoints.api.dependencies.auth_selector` (DEPRECATED shim) НЕ импортируется больше нигде, кроме DSL `security.py:52` (D-A7-01). | A4 | Dead-code removal для shim. |
| **A8-Workflow-Temporal**: проверить, что `engine/processors/invoke_workflow.py` (62 LOC) корректно registered + Temporal activity decorator pattern применим. | A8 | Workflow processor в DSL. |
| **A9-Agents-AI-RAG**: проверить, что `agent_dsl/*` (10 процессоров) registered через `@processor` для LSP/AsyncAPI. D-A7-17. | A9 | AI processors каталогизация. |
| **A11-Dependencies-Supply-Chain**: проверить, что `orjson`, `httpx`, `watchdog`, `cachetools`, `tenacity`, `pydantic` (в `pyproject.toml`) используются корректно. Некоторые моки в runtime-проверке выявили их как required. | A11 | Полный список dependencies для production-runtime. |
| **A12-Config-Environment-Ops**: проверить, что `settings.processor_pool.max_workers`, `settings.execution.timeout_default`, `settings.dlq.redis_stream` являются pydantic-settings. | A12 | Конфигурация engine-параметров. |

---

## 4. Готовность домена в %

**ИТОГОВАЯ ОЦЕНКА: 65%**

### Подробное обоснование по 5 категориям:

1. **Архитектура и core (engine + registry + middleware)**: **85%**
   - `ExecutionEngine`, `MiddlewareChain`, `ProcessorPool`, `ProcessorRegistry`, `LazyProcessorRegistry`, `PipelineValidator`, `DSLLinter`, `ExecutionTracer` — все реализованы sound с правильными async-first, thread-safety, capability-gate, fail-loud паттернами. **Но:** `_validation_cache` unbounded, double-timeout risk, validation hardcoded class names, linter hardcoded processor names.

2. **Processor registration (@processor decorator)**: **25%**
   - Только **22 из ~270+** уникальных процессоров зарегистрированы через `@processor` decorator (~8% от класса). Это блокирует schema-export, LSP-catalog, AsyncAPI-export. Решение требует циклической миграции (D-A7-08).

3. **Дубликаты и naming collisions**: **40%**
   - 9+ классов с одинаковыми именами в разных файлах (D-A7-02..D-A7-07). Naming confusion в `__init__.py` re-exports. **Частично** legacy (`external.py`, `web.py`) — candidate для удаления.

4. **Legacy / dead code**: **50%**
   - `eip/reliability.py` 442 LOC shadowed by package directory (dead).
   - `external.py` 139 LOC с дублями (частично dead).
   - `web.py` 167 LOC полностью shadowed (dead).
   - `fs_directory_scan.py` 247 LOC DEPRECATED shim, planned S175 removal.

5. **Security & production-readiness**: **70%**
   - `AuthValidateProcessor` P0 bug (D-A7-01) — broken fail-closed через удалённый `_VERIFIERS` symbol.
   - `DLQProcessor` exemplary fail-loud pattern.
   - `defusedxml` fallback в marshal (D-A7-23) — silent unsafe fallback в dev-light.
   - `auth_check()` capability-gate на processor-уровне — правильный паттерн, но `DispatchActionProcessor` НЕ gated (rely on action-handler).
   - 30+ middleware в `entrypoints/middlewares/` инвентаризированы, pure ASGI enforced.

**Формула итоговой оценки:**
- Архитектура: 85% × 0.20 = 17.0
- Registration coverage: 25% × 0.30 = 7.5
- Duplicate handling: 40% × 0.15 = 6.0
- Legacy cleanup: 50% × 0.15 = 7.5
- Security: 70% × 0.20 = 14.0
- **Итого: 52.0** (занижено из-за P0 security bug)

С коррекцией за exemplary patterns (DeadLetter, CircuitBreaker, Throttler, CallFunction, BaseAIProcessor) и sound architecture: **+13 = 65%**.

### Главные блокеры для production readiness:

1. **D-A7-01** (P0): `security.py` `_VERIFIERS_MODULE` import path broken → все `AuthValidateProcessor` fail-closed всегда. **Блокер релиза.**
2. **D-A7-08** (P1): ~94% processor-классов unregistered → schema-export, LSP-catalog, AsyncAPI неполные. **Блокер docs/LSP quality.**
3. **D-A7-04** (P1): 442 LOC legacy `eip/reliability.py` → maintenance confusion, потенциальный dead-code merge conflict. **Tech debt.**
4. **D-A7-02** (P1): Дубликаты `MCPToolProcessor`/`AgentGraphProcessor` в `external.py` shadowed → security attack surface. **Блокер security audit.**

### Рекомендованный action plan (по приоритетам):

1. **D-A7-01**: 1 час, изменить `_VERIFIERS_MODULE` на `"src.backend.core.auth.auth_selector"`. Переписать `test_auth_validate_failclosed.py` на позитивный тест.
2. **D-A7-02 + D-A7-10**: 30 мин, удалить дубль-классы из `external.py`.
3. **D-A7-04**: 5 мин, удалить `eip/reliability.py` (после проверки, что imports работают).
4. **D-A7-05**: 1 час, удалить `web.py` (после проверки, что `FillFormProcessor`/`RunScenarioProcessor` не нужны).
5. **D-A7-03 + D-A7-06 + D-A7-07**: 2 часа, переименовать дубль-классы (`FileWatchProcessor` collision, `SamplingProcessor` collision, `_BankingAIProcessor` collision).
6. **D-A7-09**: 1 час, удалить `fs_directory_scan.py` (S172 deprecation plan: S175 removal).
7. **D-A7-08**: multi-sprint initiative, мигрировать ~66 blueprint-referenced processors + ~30 часто используемых через `@processor` decorator. Цикл: storage+control_flow+components (high priority), затем AI/EIP.
8. **D-A7-15..D-A7-18**: cycle по регистрации sub-package processors.

После пунктов 1-4 → 75%+ A7 readiness. После пунктов 5-7 → 85%+.
