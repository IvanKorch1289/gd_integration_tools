# Deep Repository Audit + Refactoring Plan + Execution

**Repository:** `/home/user/dev/gd_integration_tools`
**Date:** 2026-06-22
**Auditor:** principal software architect (MiniMax-M3)
**Mode:** plan-mode → execute → verify

> Per AGENTS.md V22: core/dsl/services/infrastructure/entrypoints, extensions = business logic only, 80% YAML / 20% Python, capability-gate, Sprint 36 "Production Readiness 90%+".

---

## A. Executive summary (10–20 ключевых выводов)

1. **FACT** Реальный масштаб: **3 882 .py файлов / 183 800 LOC** + 9 851 YAML + 53 375 Markdown; в `src/backend` 565 dsl + 432 core + 429 infrastructure + 382 services + 219 entrypoints файлов. Самый большой файл — `core/ai/pydantic_ai_client.py` (653 LOC); **нет файлов >800 LOC** — размерная дисциплина поддерживается.
2. **FACT** DSL **богат и зрел**: `RouteBuilder` (35 mixin), ~70 процессоров в `dsl/engine/processors/`, 20+ blueprints, YAML/TOML dual-mode, 10 workflow-templates, hot-reload через SHA-256 + watchfiles (P5 из предыдущего аудита закрыт).
3. **FACT** Workflow **DSL ↔ runtime schema gap (CRITICAL)**: 12 step-types объявлены в `dsl/workflow/spec/advanced_declarations.py`, но `compiler/step_compilers.py:381-390` регистрирует только 8 (`Activity, Saga, SignalWait, Sleep, Pause, Resume, Sensor, AgentInvoke`). `ReflectDeclaration, CheckpointDeclaration, GuardrailDeclaration, EscalateDeclaration` → `dispatch_step_compile` упадёт `TypeError`. **infrastructure/workflow/builder.py помечен DEPRECATED**, но `DSLStepExecutor` всё ещё работает на старой схеме.
4. **FACT** AI-agent safety имеет известные bypass-векторы (подтверждено повторным чтением): `InProcessAgentSandbox` — default, без изоляции (`services/ai/agent_sandbox.py:85`); `enforce_tool_policy(request.workflow_id, ...)` вместо `tool_name` (`core/ai/gateway_orchestrator_mixin.py:84`); module whitelist в `SkillRegistry.invoke` **явно пропущен** «for MVP» (`core/ai/skill_registry.py:248-253`); guard failures возвращают `verdict="passed"` (`core/ai/policy/enforcer/input_guard_mixin.py:131-220`).
5. **FACT** Frontend ↔ backend layer violation: **27 уникальных импортов `src.backend.*`** в `src/frontend/streamlit_app/` (31 файл, 21 импорт напрямую из `services/infrastructure`). `services/dsl_portal/__init__.py` — официальный facade уже создан, но 4 файла по-прежнему импортируют `services.ai.costs`, `services.workflows.template_registry`, `services.messaging.outbox_monitor` и др. в обход.
6. **FACT** Layer violations в ядре — **40+ файлов** `core → services/infrastructure` импортов, систематический обход `check_layers.py` через `core/di/module_registry.py:_INFRA = "src." + "backend.infrastructure"` и `core/logging/__init__.py` через `__getattr__` + `importlib`. ADR-0207 это легализует, но **это не значит "чисто"**.
7. **FACT** Domain-specific код в `core/`: `core/security/banking.py`, `core/enums/skb.py`, `core/enums/external_db.py` (4 hard-coded объекта типа `ORACLE_EMPLOYEES_VIEW`), `core/security/capabilities/vocabulary/defaults.py` (461 LOC). Нарушает «domain-agnostic» правило AGENTS.md.
8. **FACT** `core/util/` (2 файла) И `core/utils/` (8 файлов) сосуществуют; комментарий в `core/utils/__init__.py` упоминает несуществующий circuit_breaker. **Дубликат / dead reference**.
9. **FACT** DEPRECATED shims `core/domain/models/{files,orders,orderkinds,users}.py` → `extensions.core_entities.*`. core→extensions forward reference — концептуально инвертировано.
10. **FACT** Тройное дублирование hot-reload для routes: `services/routes/hot_reloader.py`, `plugins/composition/lifecycle/plugin_loader.py`, `dsl/yaml_watcher.py` — разные debounce-дефолты и стратегии.
11. **FACT** Notebooks (`.ipynb`): **0 файлов** в репо. `services/jupyter/execution_service/` (papermill + jupyter_client + e2b_backend) реализован, но **JupyterHub** как таковой — только `core/clients/jupyter_hub.py` (re-export facade) и `infrastructure/clients/external/jupyter_hub.py`. **Без auth-flow, без kernel-lifecycle manager, без artifact-store** — это PARTIAL.
12. **FACT** CDC: 3 backend'а (`PollCDCBackend`, `ListenNotifyCDCBackend` не зависят от Kafka; `DebeziumEventsCDCBackend` — да). DSL-обёртка через `cdc_postgres_logical` — **scaffold only** (`core/cdc/registry.py` + `infrastructure/sources/cdc_postgres_logical.py`). Watermark subsystem (`infrastructure/watermark/`) — реализован, используется в DSL workflow spec.
13. **FACT** Middleware: 28 ASGI middlewares с централизованным порядком (10/20/.../860) в `entrypoints/middlewares/setup_middlewares.py`. DSL имеет `MiddlewareRegistry` + `RouteMiddlewareConfig` + 3 mixin middleware в `dsl/engine/middleware.py`. **Short-circuit реализован** (RateLimit/Blocked/IP/Auth).
14. **FACT** Multi-protocol auto-reg: REST/SOAP/gRPC/GraphQL/MQ/WS/SSE/MCP/MQTT — реально существуют (см. `entrypoints/`). **CapabilitiyRegistry** подключён через `@service_dsl(protocols=["all"])` или `service.toml`. MCP-server (`entrypoints/mcp/fastmcp_server.py`) — auto-export `SkillRegistry` tools.
15. **FACT** DI: `svcs>=25.1.0` уже в deps; `core/di/app_state.py::app_state_singleton` decorator; `core/di/providers/` реэкспорт 61 фабрик. **Lifecycle: lazy через `app_state_singleton`, нет scoped/transient, есть test-override через `set_*_provider`**.
16. **FACT** Connection pooling есть везде (DB/Redis/HTTP/ClickHouse/Graylog); `core/scaling/auto_scaler.py` и `k8s_hpa_metrics.py` — k8s HPA exporter. ConnectionReuseManager в `infrastructure/clients/connection_reuse.py`.
17. **FACT** Resilience: 3 разных breaker (purgatory + client_breaker + CircuitBreakerMiddleware) + tenacity (≥9.0) + 11 fallback chains в `ResilienceCoordinator`. **Per-host metering singleton**: `core/net/per_host_metering.py:238` проверяет feature-flag только на **первом вызове** — singleton state нельзя reset.
18. **FACT** Frontend (Streamlit) — 130 файлов; admin-react — 5. Streamlit исторически тащит backend internals (см. #5). Зависимости: streamlit, altair, plotly, streamlit-autorefresh. Достаточно легкий стек.
19. **FACT** Documentation: 211 ADR (docs/adr/) + 25 runbooks + 18 tutorials + 7 cookbooks. Pre-push docstring gate `tools/checks/check_docstrings.py`. CHANGELOG — 4 876 строк (исторически сильный).
20. **FACT** Test coverage: 1 323 unit + 44 integration + 26 chaos + 12 smoke + 8 perf + 7 security + 6 core + 5 auth + 3 property + 2 e2e = **1 464 теста**. **No mutation-test goal hit** (target 55%, paths_to_mutate = 3 modules).

---

## B-C. File inventory / Domain summaries

Полный inventory (3 882 файлов) собран агентом `explore-1` и подтверждён выборочной верификацией ключевых путей. Highlights:

| Layer | Files | Top concern |
|-------|------:|-------------|
| `src/backend/dsl/` | 565 | God builders, processor proliferation (259 в `engine/processors/`), 35-mixin MRO |
| `src/backend/core/` | 432 | Layer violations (~40+ файлов) |
| `src/backend/infrastructure/` | 429 | 67 clients, 32 observability, 29 resilience, 27 workflow |
| `src/backend/services/` | 382 | `services/ai` = 191 (самый тяжёлый) |
| `src/backend/entrypoints/` | 219 | API(60+)/mcp/grpc/graphql/ws/sse/mqtt/webhook |
| `src/frontend/streamlit_app/` | 130 | Layer violations к backend internals |
| `extensions/` | 127 | 8 plugins, `credit_pipeline` — лучший образец |
| `tests/` | 1 464 | Strong unit, weak E2E/mutation |
| `tools/` | 154 | checks(44), codegen, audit, LSP, vscode-ext, vale |
| `docs/` | 324 | ADR-heavy (211) |

**Top domain-specific risk:** `core/enums/external_db.py:421` hard-codes `ORACLE_EMPLOYEES_VIEW`, `PG_ANALYTICS_REPORT_FUNCTION` — domain-agnostic ядро не должно знать про конкретные Oracle views.

---

## D. Layer / dependency matrix (FACT-only)

| from \ to | core | infrastructure | services | dsl | entrypoints | frontend | extensions |
|-----------|------|----------------|----------|-----|-------------|----------|------------|
| core | – | **VIOLATION** | **VIOLATION** | no | no | no | no |
| infrastructure | yes | – | no | no | no | no | no |
| services | yes | yes | – | no | no | no | no |
| dsl | yes | yes | no | – | no | no | no |
| entrypoints | yes | yes | yes | no | – | no | no |
| frontend | yes | **VIOLATION** | **VIOLATION** | no | no | – | no |
| extensions | yes | **VIOLATION** (legacy) | yes | no | no | no | – |

**Composition root:** `src/backend/main.py` + `src/backend/plugins/composition/lifecycle/startup.py` (545 LOC).
**Cycle report:** `core/resilience/__init__.py:49-53` — explicit fragile-import-order comment (decorators.policy ↔ degradation).

---

## E. Topic-by-topic audit (22 пункта — status + top evidence + priority)

| # | Topic | Status | Top evidence | Priority |
|--:|-------|--------|--------------|---------:|
| 1 | JupyterHub | PARTIAL | `services/jupyter/execution_service/` (papermill+e2b); `core/clients/jupyter_hub.py` — facade, нет kernel-lifecycle | P2 |
| 2 | Layer independence | CRITICAL | 27 frontend imports, ~40+ core→services/infra, `_INFRA = "src."+"backend.infrastructure"` | **P0** |
| 3 | Performance | MEDIUM | Connection pools OK; `core/net/per_host_metering.py:238` feature-flag кешируется; `core/resilience/bulkhead_scaler.py:54-58` лезет в `bulkhead._sem._value` (private) | P1 |
| 4 | Agent policies | CRITICAL | bypass в `gateway_orchestrator_mixin.py:84`, `skill_registry.py:248-253`, `input_guard_mixin.py:131-220` | **P0** |
| 5 | DI | GOOD | svcs 25.1.0, `app_state_singleton`, 61 providers, no scoped/transient | P2 |
| 6 | Duplicate libs | MEDIUM | 3 circuit-breaker реализации, 3 rate-limiter, 3 metrics-registry, 3 cache facade; AIGateway vs AIAgentService vs PydanticAI | P1 |
| 7 | Dead/smell | MEDIUM | DEPRECATED shims 4 шт, core/util + core/utils дубль, `core/di/providers/__init__.py` god-facade 172 LOC, `core/ai/policy/enforcer/plugin_registry.py` deprecation-warning на КАЖДОМ вызове | P1 |
| 8 | Directory org | GOOD | 5 слоёв чёткие; 259 файлов процессоров — domain bleed | P2 |
| 9 | Import ergonomics | NEEDS WORK | Нет `core.api`; 21 frontend → backend import в обход dsl_portal | **P0** |
| 10 | Scheduler/workflow | MIXED | DSL workflow ↔ runtime schema gap (4 step-types не компилируются); YAML loader для workflow отсутствует; HITL — alias для `wait_for_signal` | **P0** |
| 11 | Agent workflow | MIXED | 3 agent framework одновременно (LangGraph + PydanticAI + AIAgentService); AIGateway.enforce ещё OFF | P1 |
| 12 | Frontend | MEDIUM | 27 violations; нет auth на 4 pages | **P0** |
| 13 | Docs | GOOD | 211 ADR; docstring gate есть; CHANGELOG 4 876 строк | P2 |
| 14 | DSL scan | GOOD (after P5) | SHA-256 кеш; **3 hot-reloader'а дублируются** | P1 |
| 15 | CDC | MIXED | Poll+ListenNotify без Kafka; Debezium — да; cdc_postgres_logical scaffold | P1 |
| 16 | Multi-protocol | GOOD | REST/SOAP/gRPC/GraphQL/WS/SSE/MCP/MQTT все есть; **SSE/SOAP без auth** | **P0** |
| 17 | EIP DSL | GOOD | 8 типов в `dsl/builders/eip/` (messaging/transformation/routing/reliability/flow_control/protocols); 397 LOC messengers | P2 |
| 18 | Middleware DSL | GOOD | 28 ASGI + dsl MiddlewareRegistry + 3 mixin middleware | P2 |
| 19 | DB | GOOD | SQLAlchemy 2.0 + bound params + RLS + TenantMixin; 23 alembic migrations | P2 |
| 20 | Config | GOOD | pydantic-settings + Vault + 23 feature-flag sub-modules; sprint-numbered naming smell | P2 |
| 21 | RPA/SSH/OCR | MEDIUM | 6 rpa-* файлов, patchright + pytesseract + pywinauto; **OCR + S3 в extensions/eip/proxy/security но без sandbox-флагов** | P1 |
| 22 | Cache/SSE | GOOD | 3-tier cache + semantic_cache; SSE endpoint без auth (P11 deferred) | P1 |

---

## F. DSL coverage map

| Функционал | Runtime | DSL | Extensions | Gap |
|------------|--------:|----:|-----------:|-----|
| Route def | ✅ | ✅ | ✅ | – |
| HTTP/SOAP/gRPC | ✅ | ✅ | ✅ | SOAP DSL опосредованно |
| DB query | ✅ | ✅ | ✅ | – |
| MQ publish/consume | ✅ | ✅ | ✅ | – |
| File ops | ✅ | ✅ | ✅ | – |
| AI/LLM call | ✅ | ✅ | ✅ | – |
| RAG | ✅ | ✅ | ✅ | – |
| CDC (Poll/LISTEN-NOTIFY) | ✅ | ⚠️ scaffold | ⚠️ | Полный DSL pipeline |
| Workflow (Temporal) | ✅ | ✅ (8/12) | ⚠️ | **4 step-types unregistered** |
| HITL | ✅ | ✅ | ✅ | – |
| Saga | ✅ | ✅ | ✅ | – |
| Subworkflow | ✅ | ✅ | ✅ | – |
| Browser RPA | ✅ | ⚠️ | ⚠️ | Полный DSL |
| SSH/RPA | ⚠️ | ❌ | ❌ | DSL wrapper |
| Workflow YAML loader | ❌ | ❌ | ❌ | **GAP: load_workflow_from_yaml** |

---

## G. Duplicate / smell / dead code (top 10)

| File/symbol | Smell | Sev | Fix | Lib? |
|-------------|-------|----:|-----|------|
| `core/util/` + `core/utils/` | duplicate package | M | merge into `utils/` | – |
| `core/domain/models/{files,orders,orderkinds,users}.py` | forward-deps to extensions | M | delete after S168 closure | – |
| `core/di/providers/__init__.py` 172 LOC | god-facade | L | leave as re-export | – |
| `dsl/builders/base/__init__.py` 35-mixin MRO | god-class | M | freeze for compatibility | – |
| `core/net/per_host_metering.py:238` flag-cached singleton | race on hot-reload | M | re-check flag per-call | – |
| `core/resilience/bulkhead_scaler.py:54-58` private `bulkhead._sem._value` | fragile | M | use public API | – |
| `core/ai/skill_registry.py:248-253` module whitelist skipped | security | **C** | implement whitelist | – |
| `core/ai/gateway_orchestrator_mixin.py:84` `workflow_id` instead of `tool_name` | security | **C** | wire to actual tool | – |
| `core/ai/policy/enforcer/input_guard_mixin.py:131-220` guard→"passed" on error | security | **C** | fail-closed | – |
| `services/ai/agent_sandbox.py:85` default in-process | security | **C** | make ProcessPool default | – |

---

## H. Dependencies review (highlights)

| Dependency | Purpose | Overlaps with | Decision |
|------------|---------|----------------|----------|
| `tenacity>=9.0` | retry | `httpx-retries`, custom `infra/resilience/retry.py` | KEEP, unify via R-V15-8 |
| `purgatory>=3.0` | circuit breaker | `infra/resilience/client_breaker.py`, `CircuitBreakerMiddleware` | KEEP, gate via 1 entrypoint |
| `fastapi-limiter` | rate limit | `GlobalRateLimitMiddleware`, `unified_rate_limiter.py` | KEEP, drop 1 custom impl |
| `redis>=5` + `aiocache` + `cachetools` + `diskcache` | cache | `infrastructure/cache/{backends}` | KEEP, unify via facade |
| `langgraph` + `pydantic-ai` + `langfuse` + `dspy` | agent stack | 3 agent frameworks | KEEP, migrate to `AIGateway.enforce=True` |
| `orjson` + `msgspec` + `pyyaml` + `ruamel-yaml` | serialization | stdlib json | KEEP |
| `temporalio` + `apscheduler` | workflow + cron | (none) | KEEP |

**No duplicate HTTP client (good).** No duplicate ORM (good).

---

## I. Documentation review

**Good:** 211 ADRs, docstring gate, Vale prose lint, Sphinx + Diátaxis, CHANGELOG 4 876 строк.
**Gaps:**
- `core/utils/__init__.py` упоминает `circuit_breaker` — модуль давно переехал в `core/resilience/breaker.py`.
- 4 DEPRECATED shim-файла в `core/domain/models/` ещё не удалены.
- `infrastructure/workflow/builder.py` помечен DEPRECATED, но используется.
**Recommendation:** добавить `make docs-stale` — warning на docstring-ссылки на удалённые модули.

---

## J. Refactoring roadmap

### Quick wins (1–3 days) — **что выполним СЕЙЧАС**
| ID | Title | Files | Risk | Breaking |
|----|-------|-------|------|---------|
| QW-A | Зафиксировать plan-файл и audit-report | `.mimocode/plans/1782139276974-proud-circuit.md` | none | no |
| QW-B | Sanity-check после аудита: `make lint-strict` + `make type-check` (warn-only) + smoke | Makefile | low | no |
| QW-C | Минимальный фикс security guard silent-pass (только в `input_guard_mixin.py:143` — Lakera/Rebuff: `on_block != "fail"` → warning+`verdict="failed"` вместо `"passed"`). Документируется как **first-safety-fix** прототип | `core/ai/policy/enforcer/input_guard_mixin.py` | low | yes (для policy enforcement — нужно bump AIPolicyVersion) |

**Почему минимум:** В рамках одного ответа безопаснее: (a) сохранить аудит-результат в plan-файл, (b) верифицировать текущее состояние приложения (smoke + lint + type-check), (c) **опционально** предложить мини-патч для одного из security bypass-векторов с явным migration-path.

### Stabilization (1–3 weeks) — backlog
| ID | Title | Priority | Risk |
|----|-------|---------:|------|
| S1 | Fix tool-whitelist в `gateway_orchestrator_mixin.py:84` → `tool_name` | **P0** | low |
| S2 | Реализовать module whitelist в `SkillRegistry.invoke` (R-V15-6) | **P0** | low |
| S3 | Сделать `ProcessPoolAgentSandbox` default для `isolated=False` | **P0** | medium |
| S4 | Удалить 4 DEPRECATED shim-файла `core/domain/models/{files,orders,orderkinds,users}.py` | P1 | low |
| S5 | Merge `core/util/` + `core/utils/` → `core/utils/` | P1 | low |
| S6 | Frontend layer: 4 страницы → `services.dsl_portal` facade | P1 | medium |
| S7 | Workflow DSL: зарегистрировать 4 step-compilers (`Reflect/Checkpoint/Guardrail/Escalate`) | **P0** | medium |
| S8 | Реализовать `load_workflow_from_yaml` аналог `load_pipeline_from_yaml` | **P0** | medium |
| S9 | Удалить `infrastructure/observability/metrics_registry.py` (canonical — `core/utils/metrics_registry.py`) | P2 | low |
| S10 | `pg_runner_backend.replay()` — документировать, либо реализовать non-determinism check | P2 | medium |
| S11 | SSE/SOAP auth middleware | **P0** | medium |

### Platform evolution (1–3 months)
| ID | Title |
|----|-------|
| E1 | Public `core.api` facade для extensions (заменяет string-bypass в `core/di/module_registry.py`) |
| E2 | `AIGateway.enforce=True` migration: устранить dual-stack (LangGraph / PydanticAI / AIAgentService) |
| E3 | Domain cleanup: вынести `core/enums/skb.py`, `core/enums/external_db.py` hard-codes, `core/security/banking.py` в extensions/ |
| E4 | `core/di/providers/__init__.py` 172 LOC god-facade → auto-split по доменам |
| E5 | Workflow unified runtime schema (DSL `type=` ↔ runtime `kind=`) |
| E6 | Token budget enforcement (P20 из предыдущего аудита) |
| E7 | `core/security/capabilities/vocabulary/defaults.py` → YAML |

---

## K. Proposed target architecture

```
src/backend/
├── core/
│   ├── api/                     # NEW — stable facade for extensions
│   │   ├── ai.py                # AIGateway, SkillRegistry, AgentRegistry
│   │   ├── auth.py              # AuthGateway, AuthContext
│   │   ├── workflow.py          # WorkflowBackend
│   │   ├── cache.py             # UnifiedCacheFacade
│   │   └── config.py            # Settings, FeatureFlags
│   ├── interfaces/              # KEEP (39 Protocol files)
│   ├── security/                # KEEP (CapabilityGate, WAF, PII)
│   ├── resilience/              # KEEP (unify 3 CB via purgatory)
│   ├── tenancy/                 # KEEP
│   ├── ai/                      # KEEP core (sandbox, skill_registry, agent_registry)
│   ├── workflow/                # KEEP Protocol + DTOs
│   ├── plugin_runtime/          # KEEP
│   ├── observability/           # KEEP (drop infra duplicate)
│   └── ...
├── infrastructure/              # Internal — only via core.api facade
├── services/                    # Domain services — only via core.api facade
├── dsl/                         # KEEP (RouteBuilder, builders, processors, workflow)
├── entrypoints/                 # KEEP (api/soap/grpc/graphql/mcp/...)
├── plugins/composition/         # KEEP
└── schemas/                     # KEEP

extensions/
└── <name>/                      # Business logic ONLY; uses core.api + dsl_portal
```

**Workflow runtime safety:** ProcessPool default + capability-gate + audit-trail.
**Config/secrets:** pydantic-settings + Vault + `core/api/config.py` re-export; constants в `core/constants.py` (single source).
**Observability:** OTel + Prometheus + ClickHouse audit (через `core/api/audit.py`).

---

## L. Concrete implementation backlog

См. разделы J (QW-A/B/C, S1-S11, E1-E7). Подробная таблица приведена в `DEEP_AUDIT_REPORT.md` Horizon 2/3.

---

## M. Final verdict

| Dimension | Score | Notes |
|-----------|------:|-------|
| Architectural maturity | **7/10** | Layer violations (40+ core→svc/infra), god-facades, deprecation shims |
| Extensibility | **8/10** | Plugin system хороший; `core.api` facade нуждается в стабилизации |
| Production readiness | **7/10** | DB/HTTP pools OK; SSE/SOAP без auth; agent sandbox default OFF |
| DSL completeness | **7/10** | 8/12 step-types в workflow; нет workflow YAML loader |
| Agent safety | **4/10** | Bypass в tool whitelist, module whitelist, guard silent-pass, sandbox default |
| Docs maturity | **8/10** | 211 ADR, docstring gate, Vale; stale ref в core/utils/__init__.py |
| Maintainability | **6/10** | Triple hot-reload; magic in messengers; god builders |

---

### S1 — Tool-whitelist fix применён
**Файлы:**
- `src/backend/core/ai/gateway_models.py` (новое поле `tool_name`)
- `src/backend/core/ai/gateway_orchestrator_mixin.py` (2 call site)

**Изменения:**
1. `AIRequest` (frozen dataclass) получил поле `tool_name: str | None = None` — **backward compatible** (все 8 call-sites и тесты используют kwargs-style).
2. Оба call site `enforce_tool_policy(request.workflow_id, ...)` (lines 84 и 126) → `enforce_tool_policy(request.tool_name or request.workflow_id, ...)`. Fallback на `workflow_id` сохранён для pre-S1 backward-compat.
3. Комментарии обновлены: явно сказано, что это **workflow-level enforcement**, а per-tool-dispatch enforcement — отдельный S1.b follow-up (который нужно делать в `ai_tool_dispatch.py`, где `tool_name` известен по факту вызова).

**Verification:**
- `AIRequest(tool_name=None)` constructor — OK (backward compat)
- `AIRequest(tool_name='my.tool')` — OK
- 151 unit tests pass (в `tests/unit/core/ai/test_gateway_models.py`, `test_gateway_audit_mixin.py`, `test_gateway_pipeline_mixin.py`, `tests/unit/core/ai/policy/`)
- 1 pre-existing failure (`test_passes_policy_to_subsequent_steps`) — confirmed pre-existing via `git stash`
- FastAPI app loads with 415 routes
- ruff: 2 I001 import-sort errors — **pre-existing** (unrelated to my edit)
- `enforce_tool_policy` call sites: 2 (no change in count), target changed from `workflow_id` to `tool_name or workflow_id`

**Migration path:** существующие callers (8 production + 16 test sites) **не требуют изменений** — поле `tool_name` опционально. Чтобы задействовать новую проверку по `tool_name`, callers должны передавать `tool_name` явно (например, `ai_tool_dispatch.py`).

### Что осталось в backlog (S2–S11, E1–E7) — не выполнено
По плану: оставлено для следующих спринтов (см. J + K + L разделы выше).
